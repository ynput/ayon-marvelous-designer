var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Marvelous Designer Addon","text":"<p>This is an AYON addon for Marvelous Designer</p> <p>API docs: https://developer.marvelousdesigner.com/</p>"},{"location":"license.html","title":"License","text":"<pre><code>                             Apache License\n                       Version 2.0, January 2004\n                    http://www.apache.org/licenses/\n</code></pre> <p>TERMS AND CONDITIONS FOR USE, REPRODUCTION, AND DISTRIBUTION</p> <ol> <li> <p>Definitions.</p> <p>\"License\" shall mean the terms and conditions for use, reproduction,   and distribution as defined by Sections 1 through 9 of this document.</p> <p>\"Licensor\" shall mean the copyright owner or entity authorized by   the copyright owner that is granting the License.</p> <p>\"Legal Entity\" shall mean the union of the acting entity and all   other entities that control, are controlled by, or are under common   control with that entity. For the purposes of this definition,   \"control\" means (i) the power, direct or indirect, to cause the   direction or management of such entity, whether by contract or   otherwise, or (ii) ownership of fifty percent (50%) or more of the   outstanding shares, or (iii) beneficial ownership of such entity.</p> <p>\"You\" (or \"Your\") shall mean an individual or Legal Entity   exercising permissions granted by this License.</p> <p>\"Source\" form shall mean the preferred form for making modifications,   including but not limited to software source code, documentation   source, and configuration files.</p> <p>\"Object\" form shall mean any form resulting from mechanical   transformation or translation of a Source form, including but   not limited to compiled object code, generated documentation,   and conversions to other media types.</p> <p>\"Work\" shall mean the work of authorship, whether in Source or   Object form, made available under the License, as indicated by a   copyright notice that is included in or attached to the work   (an example is provided in the Appendix below).</p> <p>\"Derivative Works\" shall mean any work, whether in Source or Object   form, that is based on (or derived from) the Work and for which the   editorial revisions, annotations, elaborations, or other modifications   represent, as a whole, an original work of authorship. For the purposes   of this License, Derivative Works shall not include works that remain   separable from, or merely link (or bind by name) to the interfaces of,   the Work and Derivative Works thereof.</p> <p>\"Contribution\" shall mean any work of authorship, including   the original version of the Work and any modifications or additions   to that Work or Derivative Works thereof, that is intentionally   submitted to Licensor for inclusion in the Work by the copyright owner   or by an individual or Legal Entity authorized to submit on behalf of   the copyright owner. For the purposes of this definition, \"submitted\"   means any form of electronic, verbal, or written communication sent   to the Licensor or its representatives, including but not limited to   communication on electronic mailing lists, source code control systems,   and issue tracking systems that are managed by, or on behalf of, the   Licensor for the purpose of discussing and improving the Work, but   excluding communication that is conspicuously marked or otherwise   designated in writing by the copyright owner as \"Not a Contribution.\"</p> <p>\"Contributor\" shall mean Licensor and any individual or Legal Entity   on behalf of whom a Contribution has been received by Licensor and   subsequently incorporated within the Work.</p> </li> <li> <p>Grant of Copyright License. Subject to the terms and conditions of       this License, each Contributor hereby grants to You a perpetual,       worldwide, non-exclusive, no-charge, royalty-free, irrevocable       copyright license to reproduce, prepare Derivative Works of,       publicly display, publicly perform, sublicense, and distribute the       Work and such Derivative Works in Source or Object form.</p> </li> <li> <p>Grant of Patent License. Subject to the terms and conditions of       this License, each Contributor hereby grants to You a perpetual,       worldwide, non-exclusive, no-charge, royalty-free, irrevocable       (except as stated in this section) patent license to make, have made,       use, offer to sell, sell, import, and otherwise transfer the Work,       where such license applies only to those patent claims licensable       by such Contributor that are necessarily infringed by their       Contribution(s) alone or by combination of their Contribution(s)       with the Work to which such Contribution(s) was submitted. If You       institute patent litigation against any entity (including a       cross-claim or counterclaim in a lawsuit) alleging that the Work       or a Contribution incorporated within the Work constitutes direct       or contributory patent infringement, then any patent licenses       granted to You under this License for that Work shall terminate       as of the date such litigation is filed.</p> </li> <li> <p>Redistribution. You may reproduce and distribute copies of the       Work or Derivative Works thereof in any medium, with or without       modifications, and in Source or Object form, provided that You       meet the following conditions:</p> <p>(a) You must give any other recipients of the Work or       Derivative Works a copy of this License; and</p> <p>(b) You must cause any modified files to carry prominent notices       stating that You changed the files; and</p> <p>(c) You must retain, in the Source form of any Derivative Works       that You distribute, all copyright, patent, trademark, and       attribution notices from the Source form of the Work,       excluding those notices that do not pertain to any part of       the Derivative Works; and</p> <p>(d) If the Work includes a \"NOTICE\" text file as part of its       distribution, then any Derivative Works that You distribute must       include a readable copy of the attribution notices contained       within such NOTICE file, excluding those notices that do not       pertain to any part of the Derivative Works, in at least one       of the following places: within a NOTICE text file distributed       as part of the Derivative Works; within the Source form or       documentation, if provided along with the Derivative Works; or,       within a display generated by the Derivative Works, if and       wherever such third-party notices normally appear. The contents       of the NOTICE file are for informational purposes only and       do not modify the License. You may add Your own attribution       notices within Derivative Works that You distribute, alongside       or as an addendum to the NOTICE text from the Work, provided       that such additional attribution notices cannot be construed       as modifying the License.</p> <p>You may add Your own copyright statement to Your modifications and   may provide additional or different license terms and conditions   for use, reproduction, or distribution of Your modifications, or   for any such Derivative Works as a whole, provided Your use,   reproduction, and distribution of the Work otherwise complies with   the conditions stated in this License.</p> </li> <li> <p>Submission of Contributions. Unless You explicitly state otherwise,       any Contribution intentionally submitted for inclusion in the Work       by You to the Licensor shall be under the terms and conditions of       this License, without any additional terms or conditions.       Notwithstanding the above, nothing herein shall supersede or modify       the terms of any separate license agreement you may have executed       with Licensor regarding such Contributions.</p> </li> <li> <p>Trademarks. This License does not grant permission to use the trade       names, trademarks, service marks, or product names of the Licensor,       except as required for reasonable and customary use in describing the       origin of the Work and reproducing the content of the NOTICE file.</p> </li> <li> <p>Disclaimer of Warranty. Unless required by applicable law or       agreed to in writing, Licensor provides the Work (and each       Contributor provides its Contributions) on an \"AS IS\" BASIS,       WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or       implied, including, without limitation, any warranties or conditions       of TITLE, NON-INFRINGEMENT, MERCHANTABILITY, or FITNESS FOR A       PARTICULAR PURPOSE. You are solely responsible for determining the       appropriateness of using or redistributing the Work and assume any       risks associated with Your exercise of permissions under this License.</p> </li> <li> <p>Limitation of Liability. In no event and under no legal theory,       whether in tort (including negligence), contract, or otherwise,       unless required by applicable law (such as deliberate and grossly       negligent acts) or agreed to in writing, shall any Contributor be       liable to You for damages, including any direct, indirect, special,       incidental, or consequential damages of any character arising as a       result of this License or out of the use or inability to use the       Work (including but not limited to damages for loss of goodwill,       work stoppage, computer failure or malfunction, or any and all       other commercial damages or losses), even if such Contributor       has been advised of the possibility of such damages.</p> </li> <li> <p>Accepting Warranty or Additional Liability. While redistributing       the Work or Derivative Works thereof, You may choose to offer,       and charge a fee for, acceptance of support, warranty, indemnity,       or other liability obligations and/or rights consistent with this       License. However, in accepting such obligations, You may act only       on Your own behalf and on Your sole responsibility, not on behalf       of any other Contributor, and only if You agree to indemnify,       defend, and hold each Contributor harmless for any liability       incurred by, or claims asserted against, such Contributor by reason       of your accepting any such warranty or additional liability.</p> </li> </ol> <p>END OF TERMS AND CONDITIONS</p> <p>APPENDIX: How to apply the Apache License to your work.</p> <pre><code>  To apply the Apache License to your work, attach the following\n  boilerplate notice, with the fields enclosed by brackets \"[]\"\n  replaced with your own identifying information. (Don't include\n  the brackets!)  The text should be enclosed in the appropriate\n  comment syntax for the file format. We also recommend that a\n  file or class name and description of purpose be included on the\n  same \"printed page\" as the copyright notice for easier\n  identification within third-party archives.\n</code></pre> <p>Copyright [yyyy][name of copyright owner]</p> <p>Licensed under the Apache License, Version 2.0 (the \"License\");    you may not use this file except in compliance with the License.    You may obtain a copy of the License at</p> <pre><code>   http://www.apache.org/licenses/LICENSE-2.0\n</code></pre> <p>Unless required by applicable law or agreed to in writing, software    distributed under the License is distributed on an \"AS IS\" BASIS,    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.    See the License for the specific language governing permissions and    limitations under the License.</p>"},{"location":"autoapi/summary.html","title":"Summary","text":"<ul> <li>client<ul> <li>ayon_marvelousdesigner<ul> <li>addon</li> <li>api<ul> <li>ayon_dialog</li> <li>pipeline</li> <li>plugin</li> </ul> </li> <li>deploy<ul> <li>ayon_plugins</li> </ul> </li> <li>hooks<ul> <li>pre_install_pyside</li> <li>pre_launch_workfile</li> </ul> </li> <li>plugins<ul> <li>create<ul> <li>create_model</li> <li>create_pointcache</li> <li>create_workfile</li> <li>create_zfab</li> </ul> </li> <li>load<ul> <li>load_pointcache</li> <li>load_zfab</li> </ul> </li> <li>publish<ul> <li>collect_current_file</li> <li>collect_export_option</li> <li>collect_workfile</li> <li>extract_pointcache</li> <li>extract_zfab</li> <li>increment_workfile</li> <li>save_workfile</li> <li>validate_no_fabric</li> </ul> </li> </ul> </li> <li>version</li> </ul> </li> </ul> </li> <li>server<ul> <li>settings</li> </ul> </li> </ul>"},{"location":"autoapi/client/ayon_marvelousdesigner/index.html","title":"ayon_marvelousdesigner","text":"<p>Marvelous Designer integration package for AYON.</p> <p>This package provides integration with Marvelous Designer, including the addon class and host directory configuration.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/index.html#client.ayon_marvelousdesigner.MarvelousDesignerAddon","title":"<code>MarvelousDesignerAddon</code>","text":"<p>               Bases: <code>AYONAddon</code>, <code>IHostAddon</code></p> <p>Addon class for Marvelous Designer integration with AYON.</p> <p>This addon provides host integration capabilities for Marvelous Designer, including launch hooks, environment setup, and workfile extensions.</p> Source code in <code>client/ayon_marvelousdesigner/addon.py</code> <pre><code>class MarvelousDesignerAddon(AYONAddon, IHostAddon):\n    \"\"\"Addon class for Marvelous Designer integration with AYON.\n\n    This addon provides host integration capabilities for Marvelous Designer,\n    including launch hooks, environment setup, and workfile extensions.\n    \"\"\"\n    name = \"marvelousdesigner\"\n    version = __version__\n    host_name = \"marvelousdesigner\"\n\n    def add_implementation_envs(self, env: dict, _app: object) -&gt; None:  # noqa: PLR6301\n        \"\"\"Add environment variables specific to Marvelous Designer host.\"\"\"\n        env.pop(\"QT_AUTO_SCREEN_SCALE_FACTOR\", None)\n\n    def get_launch_hook_paths(self, app: object) -&gt; list[str]:\n        \"\"\"Get paths to launch hook directories for Marvelous Designer.\n\n        Args:\n            app: Application object containing host information.\n\n        Returns:\n            List of paths to hook directories, or empty list if not applicable.\n        \"\"\"\n        if app.host_name != self.host_name:\n            return []\n        return [\n            os.path.join(MARVELOUS_DESIGNER_HOST_DIR, \"hooks\")\n        ]\n\n    def get_workfile_extensions(self) -&gt; list[str]:  # noqa: PLR6301\n        \"\"\"Get supported workfile extensions for Marvelous Designer.\n\n        Returns:\n            List of supported workfile extensions.\n        \"\"\"\n        return [\".zprj\"]\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/index.html#client.ayon_marvelousdesigner.MarvelousDesignerAddon.add_implementation_envs","title":"<code>add_implementation_envs(env, _app)</code>","text":"<p>Add environment variables specific to Marvelous Designer host.</p> Source code in <code>client/ayon_marvelousdesigner/addon.py</code> <pre><code>def add_implementation_envs(self, env: dict, _app: object) -&gt; None:  # noqa: PLR6301\n    \"\"\"Add environment variables specific to Marvelous Designer host.\"\"\"\n    env.pop(\"QT_AUTO_SCREEN_SCALE_FACTOR\", None)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/index.html#client.ayon_marvelousdesigner.MarvelousDesignerAddon.get_launch_hook_paths","title":"<code>get_launch_hook_paths(app)</code>","text":"<p>Get paths to launch hook directories for Marvelous Designer.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <code>object</code> <p>Application object containing host information.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>List of paths to hook directories, or empty list if not applicable.</p> Source code in <code>client/ayon_marvelousdesigner/addon.py</code> <pre><code>def get_launch_hook_paths(self, app: object) -&gt; list[str]:\n    \"\"\"Get paths to launch hook directories for Marvelous Designer.\n\n    Args:\n        app: Application object containing host information.\n\n    Returns:\n        List of paths to hook directories, or empty list if not applicable.\n    \"\"\"\n    if app.host_name != self.host_name:\n        return []\n    return [\n        os.path.join(MARVELOUS_DESIGNER_HOST_DIR, \"hooks\")\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/index.html#client.ayon_marvelousdesigner.MarvelousDesignerAddon.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Get supported workfile extensions for Marvelous Designer.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of supported workfile extensions.</p> Source code in <code>client/ayon_marvelousdesigner/addon.py</code> <pre><code>def get_workfile_extensions(self) -&gt; list[str]:  # noqa: PLR6301\n    \"\"\"Get supported workfile extensions for Marvelous Designer.\n\n    Returns:\n        List of supported workfile extensions.\n    \"\"\"\n    return [\".zprj\"]\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/addon.html","title":"addon","text":"<p>Marvelous Designer integration addon for AYON.</p> <p>This module provides the MarvelousDesignerAddon class which integrates Marvelous Designer host application with the AYON pipeline.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/addon.html#client.ayon_marvelousdesigner.addon.MarvelousDesignerAddon","title":"<code>MarvelousDesignerAddon</code>","text":"<p>               Bases: <code>AYONAddon</code>, <code>IHostAddon</code></p> <p>Addon class for Marvelous Designer integration with AYON.</p> <p>This addon provides host integration capabilities for Marvelous Designer, including launch hooks, environment setup, and workfile extensions.</p> Source code in <code>client/ayon_marvelousdesigner/addon.py</code> <pre><code>class MarvelousDesignerAddon(AYONAddon, IHostAddon):\n    \"\"\"Addon class for Marvelous Designer integration with AYON.\n\n    This addon provides host integration capabilities for Marvelous Designer,\n    including launch hooks, environment setup, and workfile extensions.\n    \"\"\"\n    name = \"marvelousdesigner\"\n    version = __version__\n    host_name = \"marvelousdesigner\"\n\n    def add_implementation_envs(self, env: dict, _app: object) -&gt; None:  # noqa: PLR6301\n        \"\"\"Add environment variables specific to Marvelous Designer host.\"\"\"\n        env.pop(\"QT_AUTO_SCREEN_SCALE_FACTOR\", None)\n\n    def get_launch_hook_paths(self, app: object) -&gt; list[str]:\n        \"\"\"Get paths to launch hook directories for Marvelous Designer.\n\n        Args:\n            app: Application object containing host information.\n\n        Returns:\n            List of paths to hook directories, or empty list if not applicable.\n        \"\"\"\n        if app.host_name != self.host_name:\n            return []\n        return [\n            os.path.join(MARVELOUS_DESIGNER_HOST_DIR, \"hooks\")\n        ]\n\n    def get_workfile_extensions(self) -&gt; list[str]:  # noqa: PLR6301\n        \"\"\"Get supported workfile extensions for Marvelous Designer.\n\n        Returns:\n            List of supported workfile extensions.\n        \"\"\"\n        return [\".zprj\"]\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/addon.html#client.ayon_marvelousdesigner.addon.MarvelousDesignerAddon.add_implementation_envs","title":"<code>add_implementation_envs(env, _app)</code>","text":"<p>Add environment variables specific to Marvelous Designer host.</p> Source code in <code>client/ayon_marvelousdesigner/addon.py</code> <pre><code>def add_implementation_envs(self, env: dict, _app: object) -&gt; None:  # noqa: PLR6301\n    \"\"\"Add environment variables specific to Marvelous Designer host.\"\"\"\n    env.pop(\"QT_AUTO_SCREEN_SCALE_FACTOR\", None)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/addon.html#client.ayon_marvelousdesigner.addon.MarvelousDesignerAddon.get_launch_hook_paths","title":"<code>get_launch_hook_paths(app)</code>","text":"<p>Get paths to launch hook directories for Marvelous Designer.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <code>object</code> <p>Application object containing host information.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>List of paths to hook directories, or empty list if not applicable.</p> Source code in <code>client/ayon_marvelousdesigner/addon.py</code> <pre><code>def get_launch_hook_paths(self, app: object) -&gt; list[str]:\n    \"\"\"Get paths to launch hook directories for Marvelous Designer.\n\n    Args:\n        app: Application object containing host information.\n\n    Returns:\n        List of paths to hook directories, or empty list if not applicable.\n    \"\"\"\n    if app.host_name != self.host_name:\n        return []\n    return [\n        os.path.join(MARVELOUS_DESIGNER_HOST_DIR, \"hooks\")\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/addon.html#client.ayon_marvelousdesigner.addon.MarvelousDesignerAddon.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Get supported workfile extensions for Marvelous Designer.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of supported workfile extensions.</p> Source code in <code>client/ayon_marvelousdesigner/addon.py</code> <pre><code>def get_workfile_extensions(self) -&gt; list[str]:  # noqa: PLR6301\n    \"\"\"Get supported workfile extensions for Marvelous Designer.\n\n    Returns:\n        List of supported workfile extensions.\n    \"\"\"\n    return [\".zprj\"]\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/version.html","title":"version","text":"<p>Package declaring AYON addon 'marvelous_designer' version.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html","title":"api","text":"<p>Marvelous Designer API package.</p> <p>This package provides the API interface for Marvelous Designer integration.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html#client.ayon_marvelousdesigner.api.MarvelousDesignerHost","title":"<code>MarvelousDesignerHost</code>","text":"<p>               Bases: <code>HostBase</code>, <code>IWorkfileHost</code>, <code>ILoadHost</code>, <code>IPublishHost</code></p> <p>Host class for Marvelous Designer integration with AYON pipeline.</p> <p>This class provides the main interface between AYON and Marvelous Designer, implementing workfile operations, loading, and publishing functionality.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>The host name identifier.</p> <code>_has_been_setup</code> <code>bool</code> <p>Flag indicating if the host has been</p> <code>callbacks</code> <code>list</code> <p>List of registered callbacks.</p> <code>shelves</code> <code>list</code> <p>List of UI shelves.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>class MarvelousDesignerHost(HostBase, IWorkfileHost, ILoadHost, IPublishHost):\n    \"\"\"Host class for Marvelous Designer integration with AYON pipeline.\n\n    This class provides the main interface between AYON and Marvelous Designer,\n    implementing workfile operations, loading, and publishing functionality.\n\n    Attributes:\n        name (str): The host name identifier.\n        _has_been_setup (bool): Flag indicating if the host has been\n        initialized.\n        callbacks (list): List of registered callbacks.\n        shelves (list): List of UI shelves.\n    \"\"\"\n    name = \"marvelousdesigner\"\n\n    def __init__(self):\n        \"\"\"Initialize the Marvelous Designer host with default settings.\"\"\"\n        super().__init__()\n        self._has_been_setup = False\n        self.callbacks = []\n        self.shelves = []\n\n    @staticmethod\n    def show_tools_dialog() -&gt; None:\n        \"\"\"Show tools dialog with actions leading to show other tools.\"\"\"\n        show_tools_dialog()\n\n    def install(self) -&gt; None:\n        \"\"\"Install and register the MD host with Ayon pipeline.\"\"\"\n        pyblish.api.register_host(\"marvelousdesigner\")\n\n        pyblish.api.register_plugin_path(str(PUBLISH_PATH))\n        register_loader_plugin_path(str(LOAD_PATH))\n        register_creator_plugin_path(str(CREATE_PATH))\n\n        self._has_been_setup = True\n\n    def workfile_has_unsaved_changes(self) -&gt; bool:  # noqa: PLR6301\n        \"\"\"Check if the current workfile has unsaved changes.\n\n        Returns:\n            bool: True if there are unsaved changes, False otherwise.\n        \"\"\"\n        # API not supported for the check\n        return utility_api.CheckZPRJForUnsavedChanges()\n\n    def get_workfile_extensions(self) -&gt; list[str]:  # noqa: PLR6301\n        \"\"\"Get the list of supported workfile extensions.\n\n        Returns:\n            list[str]: List of supported workfile extensions.\n        \"\"\"\n        return [\".zprj\"]\n\n    def save_workfile(self, dst_path: str | None = None) -&gt; str | None:  # noqa: PLR6301\n        \"\"\"Save the current workfile to the specified destination path.\n\n        Args:\n            dst_path (str | None, optional): Destination path to save\n                the workfile. Defaults to None.\n\n        Returns:\n            str | None: The path where the workfile was saved, or None\n            if saving failed.\n        \"\"\"\n        save_workfile(dst_path)\n        return dst_path\n\n    def open_workfile(self, filepath: str) -&gt; None:  # noqa: PLR6301\n        \"\"\"Open a workfile from the specified file path.\"\"\"\n        open_workfile(filepath)\n\n    def get_current_workfile(self) -&gt; str:  # noqa: PLR6301\n        \"\"\"Get the current workfile path from the host.\n\n        Returns:\n            str: The current workfile path.\n        \"\"\"\n        return utility_api.GetProjectFilePath()\n\n    def get_containers(self) -&gt; list:  # noqa: PLR6301\n        \"\"\"Get the list of containers in the current scene.\n\n        Returns:\n            list: List of container metadata dictionaries.\n        \"\"\"\n        return ls()\n\n    def update_context_data(self, data: dict, changes: dict) -&gt; None:  # noqa : PLR6301\n        \"\"\"Update the context data in the current file metadata.\"\"\"\n        # Note: 'changes' parameter is part of the interface but not used in MD\n        _ = changes  # Explicitly ignore the unused parameter\n        set_metadata(AYON_CONTEXT_DATA, data)\n\n    def get_context_data(self) -&gt; dict:  # noqa: PLR6301\n        \"\"\"Get the context data from the current file metadata.\n\n        Returns:\n            dict: Context data dictionary.\n        \"\"\"\n        metadata = get_ayon_metadata() or {}\n        return metadata.get(AYON_CONTEXT_DATA, {})\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html#client.ayon_marvelousdesigner.api.MarvelousDesignerHost.__init__","title":"<code>__init__()</code>","text":"<p>Initialize the Marvelous Designer host with default settings.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialize the Marvelous Designer host with default settings.\"\"\"\n    super().__init__()\n    self._has_been_setup = False\n    self.callbacks = []\n    self.shelves = []\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html#client.ayon_marvelousdesigner.api.MarvelousDesignerHost.get_containers","title":"<code>get_containers()</code>","text":"<p>Get the list of containers in the current scene.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>List of container metadata dictionaries.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def get_containers(self) -&gt; list:  # noqa: PLR6301\n    \"\"\"Get the list of containers in the current scene.\n\n    Returns:\n        list: List of container metadata dictionaries.\n    \"\"\"\n    return ls()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html#client.ayon_marvelousdesigner.api.MarvelousDesignerHost.get_context_data","title":"<code>get_context_data()</code>","text":"<p>Get the context data from the current file metadata.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Context data dictionary.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def get_context_data(self) -&gt; dict:  # noqa: PLR6301\n    \"\"\"Get the context data from the current file metadata.\n\n    Returns:\n        dict: Context data dictionary.\n    \"\"\"\n    metadata = get_ayon_metadata() or {}\n    return metadata.get(AYON_CONTEXT_DATA, {})\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html#client.ayon_marvelousdesigner.api.MarvelousDesignerHost.get_current_workfile","title":"<code>get_current_workfile()</code>","text":"<p>Get the current workfile path from the host.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The current workfile path.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def get_current_workfile(self) -&gt; str:  # noqa: PLR6301\n    \"\"\"Get the current workfile path from the host.\n\n    Returns:\n        str: The current workfile path.\n    \"\"\"\n    return utility_api.GetProjectFilePath()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html#client.ayon_marvelousdesigner.api.MarvelousDesignerHost.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Get the list of supported workfile extensions.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of supported workfile extensions.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def get_workfile_extensions(self) -&gt; list[str]:  # noqa: PLR6301\n    \"\"\"Get the list of supported workfile extensions.\n\n    Returns:\n        list[str]: List of supported workfile extensions.\n    \"\"\"\n    return [\".zprj\"]\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html#client.ayon_marvelousdesigner.api.MarvelousDesignerHost.install","title":"<code>install()</code>","text":"<p>Install and register the MD host with Ayon pipeline.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def install(self) -&gt; None:\n    \"\"\"Install and register the MD host with Ayon pipeline.\"\"\"\n    pyblish.api.register_host(\"marvelousdesigner\")\n\n    pyblish.api.register_plugin_path(str(PUBLISH_PATH))\n    register_loader_plugin_path(str(LOAD_PATH))\n    register_creator_plugin_path(str(CREATE_PATH))\n\n    self._has_been_setup = True\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html#client.ayon_marvelousdesigner.api.MarvelousDesignerHost.open_workfile","title":"<code>open_workfile(filepath)</code>","text":"<p>Open a workfile from the specified file path.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def open_workfile(self, filepath: str) -&gt; None:  # noqa: PLR6301\n    \"\"\"Open a workfile from the specified file path.\"\"\"\n    open_workfile(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html#client.ayon_marvelousdesigner.api.MarvelousDesignerHost.save_workfile","title":"<code>save_workfile(dst_path=None)</code>","text":"<p>Save the current workfile to the specified destination path.</p> <p>Parameters:</p> Name Type Description Default <code>dst_path</code> <code>str | None</code> <p>Destination path to save the workfile. Defaults to None.</p> <code>None</code> <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: The path where the workfile was saved, or None</p> <code>str | None</code> <p>if saving failed.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def save_workfile(self, dst_path: str | None = None) -&gt; str | None:  # noqa: PLR6301\n    \"\"\"Save the current workfile to the specified destination path.\n\n    Args:\n        dst_path (str | None, optional): Destination path to save\n            the workfile. Defaults to None.\n\n    Returns:\n        str | None: The path where the workfile was saved, or None\n        if saving failed.\n    \"\"\"\n    save_workfile(dst_path)\n    return dst_path\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html#client.ayon_marvelousdesigner.api.MarvelousDesignerHost.show_tools_dialog","title":"<code>show_tools_dialog()</code>  <code>staticmethod</code>","text":"<p>Show tools dialog with actions leading to show other tools.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>@staticmethod\ndef show_tools_dialog() -&gt; None:\n    \"\"\"Show tools dialog with actions leading to show other tools.\"\"\"\n    show_tools_dialog()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html#client.ayon_marvelousdesigner.api.MarvelousDesignerHost.update_context_data","title":"<code>update_context_data(data, changes)</code>","text":"<p>Update the context data in the current file metadata.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def update_context_data(self, data: dict, changes: dict) -&gt; None:  # noqa : PLR6301\n    \"\"\"Update the context data in the current file metadata.\"\"\"\n    # Note: 'changes' parameter is part of the interface but not used in MD\n    _ = changes  # Explicitly ignore the unused parameter\n    set_metadata(AYON_CONTEXT_DATA, data)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/index.html#client.ayon_marvelousdesigner.api.MarvelousDesignerHost.workfile_has_unsaved_changes","title":"<code>workfile_has_unsaved_changes()</code>","text":"<p>Check if the current workfile has unsaved changes.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if there are unsaved changes, False otherwise.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def workfile_has_unsaved_changes(self) -&gt; bool:  # noqa: PLR6301\n    \"\"\"Check if the current workfile has unsaved changes.\n\n    Returns:\n        bool: True if there are unsaved changes, False otherwise.\n    \"\"\"\n    # API not supported for the check\n    return utility_api.CheckZPRJForUnsavedChanges()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/ayon_dialog.html","title":"ayon_dialog","text":"<p>Ayon Marvelous Designer tools dialog module.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/ayon_dialog.html#client.ayon_marvelousdesigner.api.ayon_dialog.MDBtnToolsWidget","title":"<code>MDBtnToolsWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget containing buttons which are clickable.</p> Source code in <code>client/ayon_marvelousdesigner/api/ayon_dialog.py</code> <pre><code>class MDBtnToolsWidget(QtWidgets.QWidget):\n    \"\"\"Widget containing buttons which are clickable.\"\"\"\n    tool_required = QtCore.Signal(str)\n\n    def __init__(self, parent=None):  # noqa: ANN001\n        \"\"\"Ayon Marvelous Designer tools widget.\n\n        Args:\n            parent (QMainWindow, optional): Parent widget. Defaults to None.\n        \"\"\"\n        super(MDBtnToolsWidget, self).__init__(parent)  # noqa: UP008\n\n        load_btn = QtWidgets.QPushButton(\"Load...\", self)\n        manage_btn = QtWidgets.QPushButton(\"Manage...\", self)\n        publish_btn = QtWidgets.QPushButton(\"Publish...\", self)\n        workfile_btn = QtWidgets.QPushButton(\"Workfile...\", self)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(load_btn, 0)\n        layout.addWidget(manage_btn, 0)\n        layout.addWidget(publish_btn, 0)\n        layout.addWidget(workfile_btn, 0)\n        layout.addStretch(1)\n\n        load_btn.clicked.connect(self._on_load)\n        manage_btn.clicked.connect(self._on_manage)\n        publish_btn.clicked.connect(self._on_publish)\n        workfile_btn.clicked.connect(self._on_workfile)\n\n    def _on_load(self) -&gt; None:\n        self.tool_required.emit(\"loader\")\n\n    def _on_publish(self) -&gt; None:\n        self.tool_required.emit(\"publisher\")\n\n    def _on_manage(self) -&gt; None:\n        self.tool_required.emit(\"sceneinventory\")\n\n    def _on_workfile(self) -&gt; None:\n        self.tool_required.emit(\"workfiles\")\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/ayon_dialog.html#client.ayon_marvelousdesigner.api.ayon_dialog.MDBtnToolsWidget.__init__","title":"<code>__init__(parent=None)</code>","text":"<p>Ayon Marvelous Designer tools widget.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QMainWindow</code> <p>Parent widget. Defaults to None.</p> <code>None</code> Source code in <code>client/ayon_marvelousdesigner/api/ayon_dialog.py</code> <pre><code>def __init__(self, parent=None):  # noqa: ANN001\n    \"\"\"Ayon Marvelous Designer tools widget.\n\n    Args:\n        parent (QMainWindow, optional): Parent widget. Defaults to None.\n    \"\"\"\n    super(MDBtnToolsWidget, self).__init__(parent)  # noqa: UP008\n\n    load_btn = QtWidgets.QPushButton(\"Load...\", self)\n    manage_btn = QtWidgets.QPushButton(\"Manage...\", self)\n    publish_btn = QtWidgets.QPushButton(\"Publish...\", self)\n    workfile_btn = QtWidgets.QPushButton(\"Workfile...\", self)\n\n    layout = QtWidgets.QVBoxLayout(self)\n    layout.setContentsMargins(0, 0, 0, 0)\n    layout.addWidget(load_btn, 0)\n    layout.addWidget(manage_btn, 0)\n    layout.addWidget(publish_btn, 0)\n    layout.addWidget(workfile_btn, 0)\n    layout.addStretch(1)\n\n    load_btn.clicked.connect(self._on_load)\n    manage_btn.clicked.connect(self._on_manage)\n    publish_btn.clicked.connect(self._on_publish)\n    workfile_btn.clicked.connect(self._on_workfile)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/ayon_dialog.html#client.ayon_marvelousdesigner.api.ayon_dialog.MDToolsDialog","title":"<code>MDToolsDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Dialog with tool buttons that will stay opened until user close it.</p> Source code in <code>client/ayon_marvelousdesigner/api/ayon_dialog.py</code> <pre><code>class MDToolsDialog(QtWidgets.QDialog):\n    \"\"\"Dialog with tool buttons that will stay opened until user close it.\"\"\"\n    def __init__(self, *args, **kwargs):  # noqa: ANN002, ANN003\n        \"\"\"Ayon Marvelous Designer tools dialog.\"\"\"\n        super(MDToolsDialog, self).__init__(*args, **kwargs)  # noqa: UP008\n\n        self.setWindowTitle(\"Ayon tools\")\n        icon = QtGui.QIcon(resources.get_ayon_icon_filepath())\n        self.setWindowIcon(icon)\n\n        self.setWindowFlags(\n            QtCore.Qt.Window\n            | QtCore.Qt.WindowStaysOnTopHint\n            | QtCore.Qt.WindowMinimizeButtonHint\n            | QtCore.Qt.WindowMaximizeButtonHint\n            | QtCore.Qt.WindowCloseButtonHint\n        )\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n        tools_widget = MDBtnToolsWidget(self)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.addWidget(tools_widget)\n\n        tools_widget.tool_required.connect(self._on_tool_require)\n        self._tools_widget = tools_widget\n\n        self._first_show = True\n\n    def sizeHint(self) -&gt; QtCore.QSize:  # noqa: N802\n        \"\"\"Override size hint to make dialog wider.\n\n        Returns:\n            QSize: Size hint object with modified width.\n        \"\"\"\n        result = super().sizeHint()\n        result.setWidth(result.width() * 2)\n        return result\n\n    def showEvent(self, event: QtGui.QShowEvent) -&gt; None:  # noqa: N802\n        \"\"\"Handle show event for the dialog.\n\n        Applies stylesheet on first show to ensure proper styling.\n\n        Args:\n            event: The show event from Qt framework.\n        \"\"\"\n        super().showEvent(event)\n        if self._first_show:\n            self.setStyleSheet(style.load_stylesheet())\n            self._first_show = False\n\n    def closeEvent(self, event: QtGui.QCloseEvent) -&gt; None:  # noqa: N802\n        \"\"\"Handle close event for the dialog.\n\n        Cleans up the dialog reference in WindowCache upon closing.\n\n        Args:\n            event: The close event from Qt framework.\n        \"\"\"\n        super().closeEvent(event)\n        utility_api.ResetWidgetRegistry()\n\n    def _on_tool_require(self, tool_name: str) -&gt; None:\n        host_tools.show_tool_by_name(tool_name, parent=self)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/ayon_dialog.html#client.ayon_marvelousdesigner.api.ayon_dialog.MDToolsDialog.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>Ayon Marvelous Designer tools dialog.</p> Source code in <code>client/ayon_marvelousdesigner/api/ayon_dialog.py</code> <pre><code>def __init__(self, *args, **kwargs):  # noqa: ANN002, ANN003\n    \"\"\"Ayon Marvelous Designer tools dialog.\"\"\"\n    super(MDToolsDialog, self).__init__(*args, **kwargs)  # noqa: UP008\n\n    self.setWindowTitle(\"Ayon tools\")\n    icon = QtGui.QIcon(resources.get_ayon_icon_filepath())\n    self.setWindowIcon(icon)\n\n    self.setWindowFlags(\n        QtCore.Qt.Window\n        | QtCore.Qt.WindowStaysOnTopHint\n        | QtCore.Qt.WindowMinimizeButtonHint\n        | QtCore.Qt.WindowMaximizeButtonHint\n        | QtCore.Qt.WindowCloseButtonHint\n    )\n    self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n    tools_widget = MDBtnToolsWidget(self)\n\n    layout = QtWidgets.QVBoxLayout(self)\n    layout.addWidget(tools_widget)\n\n    tools_widget.tool_required.connect(self._on_tool_require)\n    self._tools_widget = tools_widget\n\n    self._first_show = True\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/ayon_dialog.html#client.ayon_marvelousdesigner.api.ayon_dialog.MDToolsDialog.closeEvent","title":"<code>closeEvent(event)</code>","text":"<p>Handle close event for the dialog.</p> <p>Cleans up the dialog reference in WindowCache upon closing.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>QCloseEvent</code> <p>The close event from Qt framework.</p> required Source code in <code>client/ayon_marvelousdesigner/api/ayon_dialog.py</code> <pre><code>def closeEvent(self, event: QtGui.QCloseEvent) -&gt; None:  # noqa: N802\n    \"\"\"Handle close event for the dialog.\n\n    Cleans up the dialog reference in WindowCache upon closing.\n\n    Args:\n        event: The close event from Qt framework.\n    \"\"\"\n    super().closeEvent(event)\n    utility_api.ResetWidgetRegistry()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/ayon_dialog.html#client.ayon_marvelousdesigner.api.ayon_dialog.MDToolsDialog.showEvent","title":"<code>showEvent(event)</code>","text":"<p>Handle show event for the dialog.</p> <p>Applies stylesheet on first show to ensure proper styling.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>QShowEvent</code> <p>The show event from Qt framework.</p> required Source code in <code>client/ayon_marvelousdesigner/api/ayon_dialog.py</code> <pre><code>def showEvent(self, event: QtGui.QShowEvent) -&gt; None:  # noqa: N802\n    \"\"\"Handle show event for the dialog.\n\n    Applies stylesheet on first show to ensure proper styling.\n\n    Args:\n        event: The show event from Qt framework.\n    \"\"\"\n    super().showEvent(event)\n    if self._first_show:\n        self.setStyleSheet(style.load_stylesheet())\n        self._first_show = False\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/ayon_dialog.html#client.ayon_marvelousdesigner.api.ayon_dialog.MDToolsDialog.sizeHint","title":"<code>sizeHint()</code>","text":"<p>Override size hint to make dialog wider.</p> <p>Returns:</p> Name Type Description <code>QSize</code> <code>QSize</code> <p>Size hint object with modified width.</p> Source code in <code>client/ayon_marvelousdesigner/api/ayon_dialog.py</code> <pre><code>def sizeHint(self) -&gt; QtCore.QSize:  # noqa: N802\n    \"\"\"Override size hint to make dialog wider.\n\n    Returns:\n        QSize: Size hint object with modified width.\n    \"\"\"\n    result = super().sizeHint()\n    result.setWidth(result.width() * 2)\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/ayon_dialog.html#client.ayon_marvelousdesigner.api.ayon_dialog.WindowCache","title":"<code>WindowCache</code>","text":"<p>Cached objects and methods to be used in global scope.</p> Source code in <code>client/ayon_marvelousdesigner/api/ayon_dialog.py</code> <pre><code>class WindowCache:\n    \"\"\"Cached objects and methods to be used in global scope.\"\"\"\n    dialog: MDToolsDialog | None = None\n    _popup = None\n    _first_show = True\n\n    @classmethod\n    def show_dialog(cls) -&gt; None:\n        \"\"\"Show the tools dialog window.\n\n        Creates a new dialog instance if none exists, then shows, raises,\n        and activates the dialog window.\n        \"\"\"\n        with qt_app_context():\n            if cls.dialog is None:\n                cls.dialog = MDToolsDialog()\n\n            cls.dialog.show()\n            cls.dialog.raise_()\n            cls.dialog.activateWindow()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/ayon_dialog.html#client.ayon_marvelousdesigner.api.ayon_dialog.WindowCache.show_dialog","title":"<code>show_dialog()</code>  <code>classmethod</code>","text":"<p>Show the tools dialog window.</p> <p>Creates a new dialog instance if none exists, then shows, raises, and activates the dialog window.</p> Source code in <code>client/ayon_marvelousdesigner/api/ayon_dialog.py</code> <pre><code>@classmethod\ndef show_dialog(cls) -&gt; None:\n    \"\"\"Show the tools dialog window.\n\n    Creates a new dialog instance if none exists, then shows, raises,\n    and activates the dialog window.\n    \"\"\"\n    with qt_app_context():\n        if cls.dialog is None:\n            cls.dialog = MDToolsDialog()\n\n        cls.dialog.show()\n        cls.dialog.raise_()\n        cls.dialog.activateWindow()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/ayon_dialog.html#client.ayon_marvelousdesigner.api.ayon_dialog.show_tools_dialog","title":"<code>show_tools_dialog()</code>","text":"<p>Show the Marvelous Designer tools dialog.</p> <p>Creates and shows the tools dialog if it doesn't exist or isn't visible. The dialog provides access to Ayon tools like loader, publisher, scene inventory, and workfiles management.</p> Source code in <code>client/ayon_marvelousdesigner/api/ayon_dialog.py</code> <pre><code>def show_tools_dialog() -&gt; None:\n    \"\"\"Show the Marvelous Designer tools dialog.\n\n    Creates and shows the tools dialog if it doesn't exist or isn't visible.\n    The dialog provides access to Ayon tools like loader, publisher, scene\n    inventory, and workfiles management.\n    \"\"\"\n    if not WindowCache.dialog or not WindowCache.dialog.isVisible():\n        WindowCache.show_dialog()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html","title":"pipeline","text":"<p>Pipeline tools for Ayon Substance Designer integration.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.MarvelousDesignerHost","title":"<code>MarvelousDesignerHost</code>","text":"<p>               Bases: <code>HostBase</code>, <code>IWorkfileHost</code>, <code>ILoadHost</code>, <code>IPublishHost</code></p> <p>Host class for Marvelous Designer integration with AYON pipeline.</p> <p>This class provides the main interface between AYON and Marvelous Designer, implementing workfile operations, loading, and publishing functionality.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>The host name identifier.</p> <code>_has_been_setup</code> <code>bool</code> <p>Flag indicating if the host has been</p> <code>callbacks</code> <code>list</code> <p>List of registered callbacks.</p> <code>shelves</code> <code>list</code> <p>List of UI shelves.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>class MarvelousDesignerHost(HostBase, IWorkfileHost, ILoadHost, IPublishHost):\n    \"\"\"Host class for Marvelous Designer integration with AYON pipeline.\n\n    This class provides the main interface between AYON and Marvelous Designer,\n    implementing workfile operations, loading, and publishing functionality.\n\n    Attributes:\n        name (str): The host name identifier.\n        _has_been_setup (bool): Flag indicating if the host has been\n        initialized.\n        callbacks (list): List of registered callbacks.\n        shelves (list): List of UI shelves.\n    \"\"\"\n    name = \"marvelousdesigner\"\n\n    def __init__(self):\n        \"\"\"Initialize the Marvelous Designer host with default settings.\"\"\"\n        super().__init__()\n        self._has_been_setup = False\n        self.callbacks = []\n        self.shelves = []\n\n    @staticmethod\n    def show_tools_dialog() -&gt; None:\n        \"\"\"Show tools dialog with actions leading to show other tools.\"\"\"\n        show_tools_dialog()\n\n    def install(self) -&gt; None:\n        \"\"\"Install and register the MD host with Ayon pipeline.\"\"\"\n        pyblish.api.register_host(\"marvelousdesigner\")\n\n        pyblish.api.register_plugin_path(str(PUBLISH_PATH))\n        register_loader_plugin_path(str(LOAD_PATH))\n        register_creator_plugin_path(str(CREATE_PATH))\n\n        self._has_been_setup = True\n\n    def workfile_has_unsaved_changes(self) -&gt; bool:  # noqa: PLR6301\n        \"\"\"Check if the current workfile has unsaved changes.\n\n        Returns:\n            bool: True if there are unsaved changes, False otherwise.\n        \"\"\"\n        # API not supported for the check\n        return utility_api.CheckZPRJForUnsavedChanges()\n\n    def get_workfile_extensions(self) -&gt; list[str]:  # noqa: PLR6301\n        \"\"\"Get the list of supported workfile extensions.\n\n        Returns:\n            list[str]: List of supported workfile extensions.\n        \"\"\"\n        return [\".zprj\"]\n\n    def save_workfile(self, dst_path: str | None = None) -&gt; str | None:  # noqa: PLR6301\n        \"\"\"Save the current workfile to the specified destination path.\n\n        Args:\n            dst_path (str | None, optional): Destination path to save\n                the workfile. Defaults to None.\n\n        Returns:\n            str | None: The path where the workfile was saved, or None\n            if saving failed.\n        \"\"\"\n        save_workfile(dst_path)\n        return dst_path\n\n    def open_workfile(self, filepath: str) -&gt; None:  # noqa: PLR6301\n        \"\"\"Open a workfile from the specified file path.\"\"\"\n        open_workfile(filepath)\n\n    def get_current_workfile(self) -&gt; str:  # noqa: PLR6301\n        \"\"\"Get the current workfile path from the host.\n\n        Returns:\n            str: The current workfile path.\n        \"\"\"\n        return utility_api.GetProjectFilePath()\n\n    def get_containers(self) -&gt; list:  # noqa: PLR6301\n        \"\"\"Get the list of containers in the current scene.\n\n        Returns:\n            list: List of container metadata dictionaries.\n        \"\"\"\n        return ls()\n\n    def update_context_data(self, data: dict, changes: dict) -&gt; None:  # noqa : PLR6301\n        \"\"\"Update the context data in the current file metadata.\"\"\"\n        # Note: 'changes' parameter is part of the interface but not used in MD\n        _ = changes  # Explicitly ignore the unused parameter\n        set_metadata(AYON_CONTEXT_DATA, data)\n\n    def get_context_data(self) -&gt; dict:  # noqa: PLR6301\n        \"\"\"Get the context data from the current file metadata.\n\n        Returns:\n            dict: Context data dictionary.\n        \"\"\"\n        metadata = get_ayon_metadata() or {}\n        return metadata.get(AYON_CONTEXT_DATA, {})\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.MarvelousDesignerHost.__init__","title":"<code>__init__()</code>","text":"<p>Initialize the Marvelous Designer host with default settings.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialize the Marvelous Designer host with default settings.\"\"\"\n    super().__init__()\n    self._has_been_setup = False\n    self.callbacks = []\n    self.shelves = []\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.MarvelousDesignerHost.get_containers","title":"<code>get_containers()</code>","text":"<p>Get the list of containers in the current scene.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>List of container metadata dictionaries.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def get_containers(self) -&gt; list:  # noqa: PLR6301\n    \"\"\"Get the list of containers in the current scene.\n\n    Returns:\n        list: List of container metadata dictionaries.\n    \"\"\"\n    return ls()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.MarvelousDesignerHost.get_context_data","title":"<code>get_context_data()</code>","text":"<p>Get the context data from the current file metadata.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Context data dictionary.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def get_context_data(self) -&gt; dict:  # noqa: PLR6301\n    \"\"\"Get the context data from the current file metadata.\n\n    Returns:\n        dict: Context data dictionary.\n    \"\"\"\n    metadata = get_ayon_metadata() or {}\n    return metadata.get(AYON_CONTEXT_DATA, {})\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.MarvelousDesignerHost.get_current_workfile","title":"<code>get_current_workfile()</code>","text":"<p>Get the current workfile path from the host.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The current workfile path.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def get_current_workfile(self) -&gt; str:  # noqa: PLR6301\n    \"\"\"Get the current workfile path from the host.\n\n    Returns:\n        str: The current workfile path.\n    \"\"\"\n    return utility_api.GetProjectFilePath()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.MarvelousDesignerHost.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Get the list of supported workfile extensions.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of supported workfile extensions.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def get_workfile_extensions(self) -&gt; list[str]:  # noqa: PLR6301\n    \"\"\"Get the list of supported workfile extensions.\n\n    Returns:\n        list[str]: List of supported workfile extensions.\n    \"\"\"\n    return [\".zprj\"]\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.MarvelousDesignerHost.install","title":"<code>install()</code>","text":"<p>Install and register the MD host with Ayon pipeline.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def install(self) -&gt; None:\n    \"\"\"Install and register the MD host with Ayon pipeline.\"\"\"\n    pyblish.api.register_host(\"marvelousdesigner\")\n\n    pyblish.api.register_plugin_path(str(PUBLISH_PATH))\n    register_loader_plugin_path(str(LOAD_PATH))\n    register_creator_plugin_path(str(CREATE_PATH))\n\n    self._has_been_setup = True\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.MarvelousDesignerHost.open_workfile","title":"<code>open_workfile(filepath)</code>","text":"<p>Open a workfile from the specified file path.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def open_workfile(self, filepath: str) -&gt; None:  # noqa: PLR6301\n    \"\"\"Open a workfile from the specified file path.\"\"\"\n    open_workfile(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.MarvelousDesignerHost.save_workfile","title":"<code>save_workfile(dst_path=None)</code>","text":"<p>Save the current workfile to the specified destination path.</p> <p>Parameters:</p> Name Type Description Default <code>dst_path</code> <code>str | None</code> <p>Destination path to save the workfile. Defaults to None.</p> <code>None</code> <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: The path where the workfile was saved, or None</p> <code>str | None</code> <p>if saving failed.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def save_workfile(self, dst_path: str | None = None) -&gt; str | None:  # noqa: PLR6301\n    \"\"\"Save the current workfile to the specified destination path.\n\n    Args:\n        dst_path (str | None, optional): Destination path to save\n            the workfile. Defaults to None.\n\n    Returns:\n        str | None: The path where the workfile was saved, or None\n        if saving failed.\n    \"\"\"\n    save_workfile(dst_path)\n    return dst_path\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.MarvelousDesignerHost.show_tools_dialog","title":"<code>show_tools_dialog()</code>  <code>staticmethod</code>","text":"<p>Show tools dialog with actions leading to show other tools.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>@staticmethod\ndef show_tools_dialog() -&gt; None:\n    \"\"\"Show tools dialog with actions leading to show other tools.\"\"\"\n    show_tools_dialog()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.MarvelousDesignerHost.update_context_data","title":"<code>update_context_data(data, changes)</code>","text":"<p>Update the context data in the current file metadata.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def update_context_data(self, data: dict, changes: dict) -&gt; None:  # noqa : PLR6301\n    \"\"\"Update the context data in the current file metadata.\"\"\"\n    # Note: 'changes' parameter is part of the interface but not used in MD\n    _ = changes  # Explicitly ignore the unused parameter\n    set_metadata(AYON_CONTEXT_DATA, data)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.MarvelousDesignerHost.workfile_has_unsaved_changes","title":"<code>workfile_has_unsaved_changes()</code>","text":"<p>Check if the current workfile has unsaved changes.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if there are unsaved changes, False otherwise.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def workfile_has_unsaved_changes(self) -&gt; bool:  # noqa: PLR6301\n    \"\"\"Check if the current workfile has unsaved changes.\n\n    Returns:\n        bool: True if there are unsaved changes, False otherwise.\n    \"\"\"\n    # API not supported for the check\n    return utility_api.CheckZPRJForUnsavedChanges()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.containerise","title":"<code>containerise(name, namespace, context, loader, options=None)</code>","text":"<p>Imprint a loaded container with metadata.</p> <p>Containerisation enables a tracking of version, author and origin for loaded assets.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of resulting assembly</p> required <code>namespace</code> <code>str</code> <p>Namespace under which to host container</p> required <code>context</code> <code>dict</code> <p>Asset information</p> required <code>loader</code> <code>LoaderPlugin</code> <p>loader instance used to produce container.</p> required <code>options</code> <code>dict</code> <p>options</p> <code>None</code> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def containerise(\n        name: str, namespace: str,\n        context: dict, loader: object,\n        options: dict | None = None) -&gt; None:\n    \"\"\"Imprint a loaded container with metadata.\n\n    Containerisation enables a tracking of version, author and origin\n    for loaded assets.\n\n    Arguments:\n        name (str): Name of resulting assembly\n        namespace (str): Namespace under which to host container\n        context (dict): Asset information\n        loader (load.LoaderPlugin): loader instance used to produce container.\n        options (dict): options\n\n    \"\"\"\n    data = {\n        \"schema\": \"ayon:container-3.0\",\n        \"id\": AYON_CONTAINER_ID,\n        \"name\": str(name),\n        \"namespace\": str(namespace) if namespace else None,\n        \"loader\": str(loader.__class__.__name__),\n        \"representation\": context[\"representation\"][\"id\"],\n        \"project_name\": context[\"project\"][\"name\"],\n        \"objectName\": name,\n    }\n    if options:\n        fabric_index = options.get(\"fabricIndex\")\n        data[\"fabricIndex\"] = fabric_index\n        data[\"objectName\"] = f\"{name}_fabric_{fabric_index}\"\n    else:\n        data[\"objectName\"] = name\n    # save the main_data in a temp folder\n    container_data = ls() or []\n    container_data.append(data)\n    set_metadata(AYON_CONTAINERS, container_data)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.get_ayon_metadata","title":"<code>get_ayon_metadata()</code>","text":"<p>Get AYON relevant metadata from current file.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>AYON metadata as a dictionary.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def get_ayon_metadata() -&gt; dict:\n    \"\"\"Get AYON relevant metadata from current file.\n\n    Returns:\n        dict: AYON metadata as a dictionary.\n    \"\"\"\n    # need to convert string to dict\n    metadata_str = utility_api.GetMetaDataForCurrentGarment()\n    return json.loads(metadata_str)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.get_current_workfile","title":"<code>get_current_workfile()</code>","text":"<p>Get the current file path from the host.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The current workfile path.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def get_current_workfile() -&gt; str:\n    \"\"\"Get the current file path from the host.\n\n    Returns:\n        str: The current workfile path.\n    \"\"\"\n    host = registered_host()\n    return host.get_current_workfile()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.get_instances","title":"<code>get_instances()</code>","text":"<p>Retrieve all stored instances from the project settings.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Dictionary of stored instances from the project settings.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def get_instances() -&gt; dict:\n    \"\"\"Retrieve all stored instances from the project settings.\n\n    Returns:\n        dict: Dictionary of stored instances from the project settings.\n    \"\"\"\n    ayon_metadata = get_ayon_metadata()\n    return ayon_metadata.get(AYON_INSTANCES, {})\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.get_instances_values","title":"<code>get_instances_values()</code>","text":"<p>Retrieve all stored instances from the project settings.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>List of all instance values from the project settings.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def get_instances_values() -&gt; list:\n    \"\"\"Retrieve all stored instances from the project settings.\n\n    Returns:\n        list: List of all instance values from the project settings.\n    \"\"\"\n    ayon_instances = get_instances()\n    return list(ayon_instances.values())\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.imprint","title":"<code>imprint(object_name, data)</code>","text":"<p>Imprint metadata onto an object in the scene.</p> <p>Parameters:</p> Name Type Description Default <code>object_name</code> <code>str</code> <p>Name of the object to imprint metadata on.</p> required <code>data</code> <code>dict</code> <p>Metadata to imprint.</p> required Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def imprint(object_name: str, data: dict) -&gt; None:\n    \"\"\"Imprint metadata onto an object in the scene.\n\n    Args:\n        object_name (str): Name of the object to imprint metadata on.\n        data (dict): Metadata to imprint.\n    \"\"\"\n    # Retrieve existing containers\n    container_data = ls()\n    # Find the container for the specified object\n    for container in container_data:\n        if container.get(\"objectName\") == object_name:\n            container.update(data)\n            break\n    else:\n        log.warning(\n            \"No container found for object %s to imprint data.\", object_name\n        )\n        return\n    # Update the metadata\n    set_metadata(AYON_CONTAINERS, container_data)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.ls","title":"<code>ls()</code>","text":"<p>List all AYON containers in the current file metadata.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>List of AYON container metadata dictionaries.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def ls() -&gt; list:\n    \"\"\"List all AYON containers in the current file metadata.\n\n    Returns:\n        list: List of AYON container metadata dictionaries.\n    \"\"\"\n    ayon_metadata = get_ayon_metadata() or {}\n    return ayon_metadata.get(AYON_CONTAINERS, [])\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.open_workfile","title":"<code>open_workfile(filepath)</code>","text":"<p>Open a workfile from the specified file path.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def open_workfile(filepath: str) -&gt; None:\n    \"\"\"Open a workfile from the specified file path.\"\"\"\n    import_options = ApiTypes.ImportZPRJOption()\n    import_api.ImportZprj(filepath, import_options)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.remove_container_data","title":"<code>remove_container_data(object_name)</code>","text":"<p>Remove container data for a specific object in the scene.</p> <p>Parameters:</p> Name Type Description Default <code>object_name</code> <code>str</code> <p>Name of the object whose container data is to be removed.</p> required Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def remove_container_data(object_name: str) -&gt; None:\n    \"\"\"Remove container data for a specific object in the scene.\n\n    Args:\n        object_name (str): Name of the object whose container data is to\n            be removed.\n    \"\"\"\n    container_data = ls()\n    # Filter out the container for the specified object\n    updated_containers = [\n        container for container in container_data\n        if container.get(\"objectName\") != object_name\n    ]\n    # Update the metadata\n    set_metadata(AYON_CONTAINERS, updated_containers)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.remove_instance","title":"<code>remove_instance(instance_id)</code>","text":"<p>Helper method to remove the data for a specific container.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def remove_instance(instance_id: str) -&gt; None:\n    \"\"\"Helper method to remove the data for a specific container.\"\"\"\n    instances = get_instances()\n    instances.pop(instance_id, None)\n    set_metadata(AYON_INSTANCES, instances)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.save_workfile","title":"<code>save_workfile(filepath)</code>","text":"<p>Save the current workfile to the specified file path.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def save_workfile(filepath: str) -&gt; None:\n    \"\"\"Save the current workfile to the specified file path.\"\"\"\n    export_api.ExportZPrj(filepath)\n    open_workfile(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.set_instance","title":"<code>set_instance(instance_id, instance_data, *, update=False)</code>","text":"<p>Set a single instance into the current file metadata.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def set_instance(\n    instance_id: str, instance_data: dict, *, update: bool = False) -&gt; None:\n    \"\"\"Set a single instance into the current file metadata.\"\"\"\n    set_instances({instance_id: instance_data}, update=update)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.set_instances","title":"<code>set_instances(instance_data_by_id, *, update=False)</code>","text":"<p>Set multiple instances into the current file metadata.</p> <p>Parameters:</p> Name Type Description Default <code>instance_data_by_id</code> <code>dict</code> <p>instance data mapped by their IDs</p> required <code>update</code> <code>bool</code> <p>Whether to update existing instances.</p> <code>False</code> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def set_instances(instance_data_by_id: dict, *, update: bool = False) -&gt; None:\n    \"\"\"Set multiple instances into the current file metadata.\n\n    Args:\n        instance_data_by_id (dict): instance data mapped by their IDs\n        update (bool, optional): Whether to update existing instances.\n        Defaults to False.\n    \"\"\"\n    instances = get_instances()\n    for instance_id, instance_data in instance_data_by_id.items():\n        if update:\n            existing_data = instances.get(instance_id, {})\n            existing_data.update(instance_data)\n        else:\n            instances[instance_id] = instance_data\n\n    set_metadata(AYON_INSTANCES, instances)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/pipeline.html#client.ayon_marvelousdesigner.api.pipeline.set_metadata","title":"<code>set_metadata(data_type, data)</code>","text":"<p>Set instance data into the current file metadata.</p> Source code in <code>client/ayon_marvelousdesigner/api/pipeline.py</code> <pre><code>def set_metadata(data_type: str, data: Union[dict, list]) -&gt; None:\n    \"\"\"Set instance data into the current file metadata.\"\"\"\n    ayon_metadata = get_ayon_metadata()\n    ayon_metadata[data_type] = data\n    # Serialize with optional formatting\n    json_to_str_data = f\"{json.dumps(ayon_metadata)}\"\n    utility_api.SetMetaDataForCurrentGarment(json_to_str_data)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/plugin.html","title":"plugin","text":"<p>Creator plugin for Marvelous Designer.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/plugin.html#client.ayon_marvelousdesigner.api.plugin.MDCreator","title":"<code>MDCreator</code>","text":"<p>               Bases: <code>Creator</code></p> <p>Marvelous Designer Creator.</p> Source code in <code>client/ayon_marvelousdesigner/api/plugin.py</code> <pre><code>class MDCreator(Creator):\n    \"\"\"Marvelous Designer Creator.\"\"\"\n    settings_category = \"marvelousdesigner\"\n\n    def create(\n            self, product_name: str,\n            instance_data: dict, pre_create_data: dict\n        ) -&gt; None:\n        \"\"\"Create a new instance in the current context.\"\"\"\n        instance = self.create_instance_in_context(product_name,\n                                                   instance_data)\n        set_instance(\n            instance_id=instance[\"instance_id\"],\n            instance_data=instance.data_to_store()\n        )\n\n    def collect_instances(self) -&gt; None:\n        \"\"\"Collect existing instances from MD and add them to the context.\n\n        This method retrieves instances that match the current creator's\n        identifier or product type and creates context instances from the\n        existing data.\n        \"\"\"\n        for instance in get_instances_values():\n            if (instance.get(\"creator_identifier\") == self.identifier or\n                    instance.get(\"productType\") == self.product_type):\n                self.create_instance_in_context_from_existing(instance)\n\n    def update_instances(self, update_list: list) -&gt; None:  # noqa: PLR6301\n        \"\"\"Update existing instances with new data.\"\"\"\n        instance_data_by_id = {}\n        for instance, _changes in update_list:\n            # Persist the data\n            instance_id = instance.get(\"instance_id\")\n            instance_data = instance.data_to_store()\n            instance_data_by_id[instance_id] = instance_data\n        set_instances(instance_data_by_id, update=True)\n\n    def remove_instances(self, instances: list) -&gt; None:\n        \"\"\"Remove instances from MD and the current context.\"\"\"\n        for instance in instances:\n            remove_instance(instance[\"instance_id\"])\n            self._remove_instance_from_context(instance)\n\n    # Helper methods (this might get moved into Creator class)\n    def create_instance_in_context(\n            self, product_name: str, data: dict) -&gt; CreatedInstance:\n        \"\"\"Create a new instance in the current context.\n\n        Args:\n            product_name (str): Name of the product.\n            data (dict): Data associated with the instance.\n\n        Returns:\n            CreatedInstance: The created instance.\n        \"\"\"\n        instance = CreatedInstance(\n            product_type=self.product_type,\n            product_name=product_name,\n            data=data,\n            creator=self\n        )\n        self.create_context.creator_adds_instance(instance)\n        return instance\n\n    def create_instance_in_context_from_existing(\n            self, data: dict) -&gt; CreatedInstance:\n        \"\"\"Create an instance in the current context from existing data.\n\n        Args:\n            data (dict): Existing instance data.\n\n        Returns:\n            CreatedInstance: The created instance.\n        \"\"\"\n        instance = CreatedInstance.from_existing(data, self)\n        self.create_context.creator_adds_instance(instance)\n        return instance\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/plugin.html#client.ayon_marvelousdesigner.api.plugin.MDCreator.collect_instances","title":"<code>collect_instances()</code>","text":"<p>Collect existing instances from MD and add them to the context.</p> <p>This method retrieves instances that match the current creator's identifier or product type and creates context instances from the existing data.</p> Source code in <code>client/ayon_marvelousdesigner/api/plugin.py</code> <pre><code>def collect_instances(self) -&gt; None:\n    \"\"\"Collect existing instances from MD and add them to the context.\n\n    This method retrieves instances that match the current creator's\n    identifier or product type and creates context instances from the\n    existing data.\n    \"\"\"\n    for instance in get_instances_values():\n        if (instance.get(\"creator_identifier\") == self.identifier or\n                instance.get(\"productType\") == self.product_type):\n            self.create_instance_in_context_from_existing(instance)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/plugin.html#client.ayon_marvelousdesigner.api.plugin.MDCreator.create","title":"<code>create(product_name, instance_data, pre_create_data)</code>","text":"<p>Create a new instance in the current context.</p> Source code in <code>client/ayon_marvelousdesigner/api/plugin.py</code> <pre><code>def create(\n        self, product_name: str,\n        instance_data: dict, pre_create_data: dict\n    ) -&gt; None:\n    \"\"\"Create a new instance in the current context.\"\"\"\n    instance = self.create_instance_in_context(product_name,\n                                               instance_data)\n    set_instance(\n        instance_id=instance[\"instance_id\"],\n        instance_data=instance.data_to_store()\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/plugin.html#client.ayon_marvelousdesigner.api.plugin.MDCreator.create_instance_in_context","title":"<code>create_instance_in_context(product_name, data)</code>","text":"<p>Create a new instance in the current context.</p> <p>Parameters:</p> Name Type Description Default <code>product_name</code> <code>str</code> <p>Name of the product.</p> required <code>data</code> <code>dict</code> <p>Data associated with the instance.</p> required <p>Returns:</p> Name Type Description <code>CreatedInstance</code> <code>CreatedInstance</code> <p>The created instance.</p> Source code in <code>client/ayon_marvelousdesigner/api/plugin.py</code> <pre><code>def create_instance_in_context(\n        self, product_name: str, data: dict) -&gt; CreatedInstance:\n    \"\"\"Create a new instance in the current context.\n\n    Args:\n        product_name (str): Name of the product.\n        data (dict): Data associated with the instance.\n\n    Returns:\n        CreatedInstance: The created instance.\n    \"\"\"\n    instance = CreatedInstance(\n        product_type=self.product_type,\n        product_name=product_name,\n        data=data,\n        creator=self\n    )\n    self.create_context.creator_adds_instance(instance)\n    return instance\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/plugin.html#client.ayon_marvelousdesigner.api.plugin.MDCreator.create_instance_in_context_from_existing","title":"<code>create_instance_in_context_from_existing(data)</code>","text":"<p>Create an instance in the current context from existing data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>Existing instance data.</p> required <p>Returns:</p> Name Type Description <code>CreatedInstance</code> <code>CreatedInstance</code> <p>The created instance.</p> Source code in <code>client/ayon_marvelousdesigner/api/plugin.py</code> <pre><code>def create_instance_in_context_from_existing(\n        self, data: dict) -&gt; CreatedInstance:\n    \"\"\"Create an instance in the current context from existing data.\n\n    Args:\n        data (dict): Existing instance data.\n\n    Returns:\n        CreatedInstance: The created instance.\n    \"\"\"\n    instance = CreatedInstance.from_existing(data, self)\n    self.create_context.creator_adds_instance(instance)\n    return instance\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/plugin.html#client.ayon_marvelousdesigner.api.plugin.MDCreator.remove_instances","title":"<code>remove_instances(instances)</code>","text":"<p>Remove instances from MD and the current context.</p> Source code in <code>client/ayon_marvelousdesigner/api/plugin.py</code> <pre><code>def remove_instances(self, instances: list) -&gt; None:\n    \"\"\"Remove instances from MD and the current context.\"\"\"\n    for instance in instances:\n        remove_instance(instance[\"instance_id\"])\n        self._remove_instance_from_context(instance)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/api/plugin.html#client.ayon_marvelousdesigner.api.plugin.MDCreator.update_instances","title":"<code>update_instances(update_list)</code>","text":"<p>Update existing instances with new data.</p> Source code in <code>client/ayon_marvelousdesigner/api/plugin.py</code> <pre><code>def update_instances(self, update_list: list) -&gt; None:  # noqa: PLR6301\n    \"\"\"Update existing instances with new data.\"\"\"\n    instance_data_by_id = {}\n    for instance, _changes in update_list:\n        # Persist the data\n        instance_id = instance.get(\"instance_id\")\n        instance_data = instance.data_to_store()\n        instance_data_by_id[instance_id] = instance_data\n    set_instances(instance_data_by_id, update=True)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/deploy/index.html","title":"deploy","text":""},{"location":"autoapi/client/ayon_marvelousdesigner/deploy/ayon_plugins.html","title":"ayon_plugins","text":"<p>Deployed script with Ayon integration for Marvelous Designer.  User needs to manually add this script into Marvelous Designer Plugins -&gt; Plug-in Managers.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/hooks/index.html","title":"hooks","text":""},{"location":"autoapi/client/ayon_marvelousdesigner/hooks/pre_install_pyside.html","title":"pre_install_pyside","text":"<p>Pre-launch hook for installing Qt bindings in Marvelous Designer.</p> <p>This module provides: - InstallQtBinding: Pre-launch hook that installs PySide6 to MD's   Python environment to enable Qt-based functionality.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/hooks/pre_install_pyside.html#client.ayon_marvelousdesigner.hooks.pre_install_pyside.InstallQtBinding","title":"<code>InstallQtBinding</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Install Qt binding to unreal's python packages.</p> Source code in <code>client/ayon_marvelousdesigner/hooks/pre_install_pyside.py</code> <pre><code>class InstallQtBinding(PreLaunchHook):\n    \"\"\"Install Qt binding to unreal's python packages.\"\"\"\n\n    app_groups: ClassVar = {\"marvelousdesigner\"}\n    order = 11\n    launch_types: ClassVar = {LaunchTypes.local}\n\n    def execute(self) -&gt; None:\n        \"\"\"Execute the pre-launch hook to install PySide6.\"\"\"\n        current_platform = platform.system().lower()\n        python_exe = (\n            \"python\"\n            if current_platform != \"windows\"\n            else \"python.exe\"\n        )\n        python_executable = Path(shutil.which(python_exe)).as_posix()\n        md_setting = self.data[\"project_settings\"][\"marvelous_designer\"]\n        qt_binding_dir = md_setting[\"prelaunch_settings\"].get(\n            \"qt_binding_dir\", \"\")\n        qt_binding_dir = Path(qt_binding_dir)\n        if not qt_binding_dir.exists():\n            self.log.warning(\n                \"Qt binding directory '%s' does not exist.\", qt_binding_dir\n            )\n            return\n\n        return_code = self.install_pyside(python_executable, qt_binding_dir)\n        if return_code:\n            self.log.info(\"PySide6 installed successfully.\")\n            self.launch_context.env[\"QtDir\"] = qt_binding_dir.as_posix()\n\n    def install_pyside(\n            self, python_executable: str,\n            qt_binding_dir: Path) -&gt; Union[int, None]:\n        \"\"\"Install PySide6 python module to marvelous designer's python.\n\n        Installation requires administration rights that's why it is required\n        to use \"pywin32\" module which can execute command's and ask for\n        administration rights.\n\n        Note:\n            This is asking for administrative right always, no matter if\n            it is actually needed or not. Unfortunately getting\n            correct permissions for directory on Windows isn't that trivial.\n            You can either use `win32security` module or run `icacls` command\n            in subprocess and parse its output.\n\n        Returns:\n            Return code from the pip install process, or None\n            if installation fails.\n\n        \"\"\"\n        args = [\n            python_executable,\n            \"-m\",\n            \"pip\",\n            \"install\",\n            \"PySide6\",\n            \"--target\",\n            qt_binding_dir.as_posix(),\n            \"--ignore-installed\",\n        ]\n\n        return self.pip_install(args)\n\n    def pip_install(self, args: list) -&gt; Union[int, None]:\n        \"\"\"Execute pip install command in subprocess.\n\n        Args:\n            args (list): List of command line arguments for pip install.\n\n        Returns:\n            bool or None: True if pip install was successful (return code 0),\n            None if an exception occurred during execution.\n\n        \"\"\"\n        try:\n            # Parameters\n            # - use \"-m pip\" as module pip to install PySide2/6 and argument\n            #   \"--ignore-installed\" is to force install module to unreal\n            #   site-packages and make sure it is binary compatible\n\n            process = subprocess.Popen(\n                args, stdout=subprocess.PIPE,\n                stderr=subprocess.PIPE, universal_newlines=True\n            )\n            process.communicate()\n\n        except PermissionError:\n            self.log.warning(\n                'Permission denied with command: \"%s\".', \" \".join(args),\n                exc_info=True)\n        except OSError as error:\n            self.log.warning(\n                'OS error has occurred: \"%s\".', error, exc_info=True)\n        except subprocess.SubprocessError:\n            pass\n        else:\n            return process.returncode == 0\n\n        return None\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/hooks/pre_install_pyside.html#client.ayon_marvelousdesigner.hooks.pre_install_pyside.InstallQtBinding.execute","title":"<code>execute()</code>","text":"<p>Execute the pre-launch hook to install PySide6.</p> Source code in <code>client/ayon_marvelousdesigner/hooks/pre_install_pyside.py</code> <pre><code>def execute(self) -&gt; None:\n    \"\"\"Execute the pre-launch hook to install PySide6.\"\"\"\n    current_platform = platform.system().lower()\n    python_exe = (\n        \"python\"\n        if current_platform != \"windows\"\n        else \"python.exe\"\n    )\n    python_executable = Path(shutil.which(python_exe)).as_posix()\n    md_setting = self.data[\"project_settings\"][\"marvelous_designer\"]\n    qt_binding_dir = md_setting[\"prelaunch_settings\"].get(\n        \"qt_binding_dir\", \"\")\n    qt_binding_dir = Path(qt_binding_dir)\n    if not qt_binding_dir.exists():\n        self.log.warning(\n            \"Qt binding directory '%s' does not exist.\", qt_binding_dir\n        )\n        return\n\n    return_code = self.install_pyside(python_executable, qt_binding_dir)\n    if return_code:\n        self.log.info(\"PySide6 installed successfully.\")\n        self.launch_context.env[\"QtDir\"] = qt_binding_dir.as_posix()\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/hooks/pre_install_pyside.html#client.ayon_marvelousdesigner.hooks.pre_install_pyside.InstallQtBinding.install_pyside","title":"<code>install_pyside(python_executable, qt_binding_dir)</code>","text":"<p>Install PySide6 python module to marvelous designer's python.</p> <p>Installation requires administration rights that's why it is required to use \"pywin32\" module which can execute command's and ask for administration rights.</p> Note <p>This is asking for administrative right always, no matter if it is actually needed or not. Unfortunately getting correct permissions for directory on Windows isn't that trivial. You can either use <code>win32security</code> module or run <code>icacls</code> command in subprocess and parse its output.</p> <p>Returns:</p> Type Description <code>Union[int, None]</code> <p>Return code from the pip install process, or None</p> <code>Union[int, None]</code> <p>if installation fails.</p> Source code in <code>client/ayon_marvelousdesigner/hooks/pre_install_pyside.py</code> <pre><code>def install_pyside(\n        self, python_executable: str,\n        qt_binding_dir: Path) -&gt; Union[int, None]:\n    \"\"\"Install PySide6 python module to marvelous designer's python.\n\n    Installation requires administration rights that's why it is required\n    to use \"pywin32\" module which can execute command's and ask for\n    administration rights.\n\n    Note:\n        This is asking for administrative right always, no matter if\n        it is actually needed or not. Unfortunately getting\n        correct permissions for directory on Windows isn't that trivial.\n        You can either use `win32security` module or run `icacls` command\n        in subprocess and parse its output.\n\n    Returns:\n        Return code from the pip install process, or None\n        if installation fails.\n\n    \"\"\"\n    args = [\n        python_executable,\n        \"-m\",\n        \"pip\",\n        \"install\",\n        \"PySide6\",\n        \"--target\",\n        qt_binding_dir.as_posix(),\n        \"--ignore-installed\",\n    ]\n\n    return self.pip_install(args)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/hooks/pre_install_pyside.html#client.ayon_marvelousdesigner.hooks.pre_install_pyside.InstallQtBinding.pip_install","title":"<code>pip_install(args)</code>","text":"<p>Execute pip install command in subprocess.</p> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>list</code> <p>List of command line arguments for pip install.</p> required <p>Returns:</p> Type Description <code>Union[int, None]</code> <p>bool or None: True if pip install was successful (return code 0),</p> <code>Union[int, None]</code> <p>None if an exception occurred during execution.</p> Source code in <code>client/ayon_marvelousdesigner/hooks/pre_install_pyside.py</code> <pre><code>def pip_install(self, args: list) -&gt; Union[int, None]:\n    \"\"\"Execute pip install command in subprocess.\n\n    Args:\n        args (list): List of command line arguments for pip install.\n\n    Returns:\n        bool or None: True if pip install was successful (return code 0),\n        None if an exception occurred during execution.\n\n    \"\"\"\n    try:\n        # Parameters\n        # - use \"-m pip\" as module pip to install PySide2/6 and argument\n        #   \"--ignore-installed\" is to force install module to unreal\n        #   site-packages and make sure it is binary compatible\n\n        process = subprocess.Popen(\n            args, stdout=subprocess.PIPE,\n            stderr=subprocess.PIPE, universal_newlines=True\n        )\n        process.communicate()\n\n    except PermissionError:\n        self.log.warning(\n            'Permission denied with command: \"%s\".', \" \".join(args),\n            exc_info=True)\n    except OSError as error:\n        self.log.warning(\n            'OS error has occurred: \"%s\".', error, exc_info=True)\n    except subprocess.SubprocessError:\n        pass\n    else:\n        return process.returncode == 0\n\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/hooks/pre_launch_workfile.html","title":"pre_launch_workfile","text":"<p>Pre-launch hook for Marvelous Designer workfile handling.</p> <p>This module provides a hook that creates a temporary Zprj file for MD when no last workfile exists or when not starting from the last workfile.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/hooks/pre_launch_workfile.html#client.ayon_marvelousdesigner.hooks.pre_launch_workfile.CreateTempZprjFile","title":"<code>CreateTempZprjFile</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Create Temp Zprj File to Marvelous Designer.</p> <p>The temp zprj file would be created in Marvelous Designer prior to the launch of the software if there is no last workfile</p> <p>Hook <code>GlobalHostDataHook</code> must be executed before this hook.</p> Source code in <code>client/ayon_marvelousdesigner/hooks/pre_launch_workfile.py</code> <pre><code>class CreateTempZprjFile(PreLaunchHook):\n    \"\"\"Create Temp Zprj File to Marvelous Designer.\n\n    The temp zprj file would be created in Marvelous Designer prior to\n    the launch of the software if there is no last workfile\n\n    Hook `GlobalHostDataHook` must be executed before this hook.\n    \"\"\"\n    app_groups: ClassVar = {\"marvelousdesigner\"}\n    order = 12\n    launch_types: ClassVar = {LaunchTypes.local}\n\n    def execute(self) -&gt; None:\n        \"\"\"Execute the pre-launch hook to create temp zprj file.\"\"\"\n        last_workfile = self.data.get(\"last_workfile_path\")\n        if self.data.get(\"start_last_workfile\")  \\\n            and last_workfile  \\\n                and os.path.exists(last_workfile):\n            self.log.info(\"It is set to start last workfile on start.\")\n        else:\n            source_template_file = os.path.join(\n                MARVELOUS_DESIGNER_HOST_DIR, \"default_zprj\", \"Untitled_MD.zprj\"\n            )\n            staging_dir = tempdir.get_temp_dir(\n                self.data[\"project_name\"],\n                use_local_temp=True\n            )\n            spm_filename = os.path.basename(source_template_file)\n            last_workfile = os.path.join(staging_dir, spm_filename)\n            shutil.copyfile(source_template_file, last_workfile)\n            self.launch_context.env[\"AYON_TEMP_DIR\"] = staging_dir\n\n        self.launch_context.launch_args.append(last_workfile)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/hooks/pre_launch_workfile.html#client.ayon_marvelousdesigner.hooks.pre_launch_workfile.CreateTempZprjFile.execute","title":"<code>execute()</code>","text":"<p>Execute the pre-launch hook to create temp zprj file.</p> Source code in <code>client/ayon_marvelousdesigner/hooks/pre_launch_workfile.py</code> <pre><code>def execute(self) -&gt; None:\n    \"\"\"Execute the pre-launch hook to create temp zprj file.\"\"\"\n    last_workfile = self.data.get(\"last_workfile_path\")\n    if self.data.get(\"start_last_workfile\")  \\\n        and last_workfile  \\\n            and os.path.exists(last_workfile):\n        self.log.info(\"It is set to start last workfile on start.\")\n    else:\n        source_template_file = os.path.join(\n            MARVELOUS_DESIGNER_HOST_DIR, \"default_zprj\", \"Untitled_MD.zprj\"\n        )\n        staging_dir = tempdir.get_temp_dir(\n            self.data[\"project_name\"],\n            use_local_temp=True\n        )\n        spm_filename = os.path.basename(source_template_file)\n        last_workfile = os.path.join(staging_dir, spm_filename)\n        shutil.copyfile(source_template_file, last_workfile)\n        self.launch_context.env[\"AYON_TEMP_DIR\"] = staging_dir\n\n    self.launch_context.launch_args.append(last_workfile)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/index.html","title":"plugins","text":""},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/index.html","title":"create","text":""},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_model.html","title":"create_model","text":"<p>Create plugin for Marvelous Designer model product type.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_model.html#client.ayon_marvelousdesigner.plugins.create.create_model.CreateModel","title":"<code>CreateModel</code>","text":"<p>               Bases: <code>MDCreator</code></p> <p>Model creator for Marvelous Designer.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/create/create_model.py</code> <pre><code>class CreateModel(plugin.MDCreator):\n    \"\"\"Model creator for Marvelous Designer.\"\"\"\n    identifier = \"io.ayon.creators.marvelousdesigner.model\"\n    label = \"Model\"\n    product_type = \"model\"\n    product_base_type = \"model\"\n    icon = \"cube\"\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_pointcache.html","title":"create_pointcache","text":"<p>Create Point Cache plugin for Marvelous Designer.</p> <p>This module contains the CreatePointCache class for creating point cache products in Marvelous Designer within the AYON pipeline.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_pointcache.html#client.ayon_marvelousdesigner.plugins.create.create_pointcache.CreatePointCache","title":"<code>CreatePointCache</code>","text":"<p>               Bases: <code>MDCreator</code></p> <p>Model creator for Marvelous Designer.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/create/create_pointcache.py</code> <pre><code>class CreatePointCache(plugin.MDCreator):\n    \"\"\"Model creator for Marvelous Designer.\"\"\"\n    identifier = \"io.ayon.creators.marvelousdesigner.pointcache\"\n    label = \"Point Cache\"\n    product_type = \"pointcache\"\n    product_base_type = \"pointcache\"\n    icon = \"pagelines\"\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_workfile.html","title":"create_workfile","text":"<p>Creator plugin for creating workfiles.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_workfile.html#client.ayon_marvelousdesigner.plugins.create.create_workfile.CreateWorkfile","title":"<code>CreateWorkfile</code>","text":"<p>               Bases: <code>AutoCreator</code></p> <p>Workfile auto-creator.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/create/create_workfile.py</code> <pre><code>class CreateWorkfile(AutoCreator):\n    \"\"\"Workfile auto-creator.\"\"\"\n    identifier = \"io.ayon.creators.marvelousdesigner.workfile\"\n    label = \"Workfile\"\n    product_type = \"workfile\"\n    product_base_type = \"workfile\"\n    icon = \"document\"\n\n    default_variant = \"Main\"\n    settings_category = \"marvelousdesigner\"\n\n    def create(self) -&gt; None:\n        \"\"\"Create or update the workfile instance in the current context.\"\"\"\n        variant = self.default_variant\n        project_name = self.project_name\n        folder_path = self.create_context.get_current_folder_path()\n        task_name = self.create_context.get_current_task_name()\n        host_name = self.create_context.host_name\n\n        # Workfile instance should always exist and must only exist once.\n        # As such we'll first check if it already exists and is collected.\n        current_instance = next(\n            (\n                instance for instance in self.create_context.instances\n                if instance.creator_identifier == self.identifier\n            ), None)\n\n        project_entity = self.create_context.get_current_project_entity()\n        folder_entity = self.create_context.get_current_folder_entity()\n        task_entity = self.create_context.get_current_task_entity()\n\n        project_name = project_entity[\"name\"]\n        folder_path = folder_entity[\"path\"]\n        task_name = task_entity[\"name\"]\n        host_name = self.create_context.host_name\n\n        current_folder_path = None\n        if current_instance is not None:\n            current_folder_path = current_instance[\"folderPath\"]\n\n        if current_instance is None:\n            self.log.info(\"Auto-creating workfile instance...\")\n            product_name = self.get_product_name(\n                project_name=project_name,\n                project_entity=project_entity,\n                folder_entity=folder_entity,\n                task_entity=task_entity,\n                variant=variant,\n                host_name=host_name,\n            )\n            data = {\n                \"folderPath\": folder_path,\n                \"task\": task_name,\n                \"variant\": variant\n            }\n            current_instance = self.create_instance_in_context(product_name,\n                                                               data)\n        elif (\n            current_folder_path != folder_path\n            or current_instance[\"task\"] != task_name\n        ):\n            # Update instance context if is not the same\n            product_name = self.get_product_name(\n                project_name=project_name,\n                project_entity=folder_entity,\n                folder_entity=folder_entity,\n                task_entity=task_entity,\n                variant=variant,\n                host_name=host_name,\n            )\n            current_instance[\"folderPath\"] = folder_path\n            current_instance[\"task\"] = task_name\n            current_instance[\"productName\"] = product_name\n        set_instance(\n            instance_id=current_instance.get(\"instance_id\"),\n            instance_data=current_instance.data_to_store()\n        )\n\n    def collect_instances(self) -&gt; None:\n        \"\"\"Collect existing instances from MD and add them to the context.\"\"\"\n        for instance in get_instances_values():\n            if (instance.get(\"creator_identifier\") == self.identifier or\n                    instance.get(\"productType\") == self.product_type):\n                self.create_instance_in_context_from_existing(instance)\n\n    def update_instances(self, update_list: list) -&gt; None:  # noqa: PLR6301\n        \"\"\"Update existing instances with new data.\"\"\"\n        instance_data_by_id = {}\n        for instance, _changes in update_list:\n            # Persist the data\n            instance_id = instance.get(\"instance_id\")\n            instance_data = instance.data_to_store()\n            instance_data_by_id[instance_id] = instance_data\n        set_instances(instance_data_by_id, update=True)\n\n    def create_instance_in_context(\n            self, product_name: str, data: dict) -&gt; CreatedInstance:\n        \"\"\"Create a new instance in the current context.\n\n        Args:\n            product_name (str): The name of the product to create.\n            data (dict): The data for the instance.\n\n        Returns:\n            CreatedInstance: The newly created instance.\n        \"\"\"\n        instance = CreatedInstance(\n            product_type=self.product_type,\n            product_name=product_name,\n            data=data,\n            creator=self\n        )\n        self.create_context.creator_adds_instance(instance)\n        return instance\n\n    def create_instance_in_context_from_existing(\n            self, data: dict) -&gt; CreatedInstance:\n        \"\"\"Create an instance in the current context from existing data.\n\n        Args:\n            data (dict): The existing instance data.\n\n        Returns:\n            CreatedInstance: The created instance from existing data.\n        \"\"\"\n        instance = CreatedInstance.from_existing(data, self)\n        self.create_context.creator_adds_instance(instance)\n        return instance\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_workfile.html#client.ayon_marvelousdesigner.plugins.create.create_workfile.CreateWorkfile.collect_instances","title":"<code>collect_instances()</code>","text":"<p>Collect existing instances from MD and add them to the context.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/create/create_workfile.py</code> <pre><code>def collect_instances(self) -&gt; None:\n    \"\"\"Collect existing instances from MD and add them to the context.\"\"\"\n    for instance in get_instances_values():\n        if (instance.get(\"creator_identifier\") == self.identifier or\n                instance.get(\"productType\") == self.product_type):\n            self.create_instance_in_context_from_existing(instance)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_workfile.html#client.ayon_marvelousdesigner.plugins.create.create_workfile.CreateWorkfile.create","title":"<code>create()</code>","text":"<p>Create or update the workfile instance in the current context.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/create/create_workfile.py</code> <pre><code>def create(self) -&gt; None:\n    \"\"\"Create or update the workfile instance in the current context.\"\"\"\n    variant = self.default_variant\n    project_name = self.project_name\n    folder_path = self.create_context.get_current_folder_path()\n    task_name = self.create_context.get_current_task_name()\n    host_name = self.create_context.host_name\n\n    # Workfile instance should always exist and must only exist once.\n    # As such we'll first check if it already exists and is collected.\n    current_instance = next(\n        (\n            instance for instance in self.create_context.instances\n            if instance.creator_identifier == self.identifier\n        ), None)\n\n    project_entity = self.create_context.get_current_project_entity()\n    folder_entity = self.create_context.get_current_folder_entity()\n    task_entity = self.create_context.get_current_task_entity()\n\n    project_name = project_entity[\"name\"]\n    folder_path = folder_entity[\"path\"]\n    task_name = task_entity[\"name\"]\n    host_name = self.create_context.host_name\n\n    current_folder_path = None\n    if current_instance is not None:\n        current_folder_path = current_instance[\"folderPath\"]\n\n    if current_instance is None:\n        self.log.info(\"Auto-creating workfile instance...\")\n        product_name = self.get_product_name(\n            project_name=project_name,\n            project_entity=project_entity,\n            folder_entity=folder_entity,\n            task_entity=task_entity,\n            variant=variant,\n            host_name=host_name,\n        )\n        data = {\n            \"folderPath\": folder_path,\n            \"task\": task_name,\n            \"variant\": variant\n        }\n        current_instance = self.create_instance_in_context(product_name,\n                                                           data)\n    elif (\n        current_folder_path != folder_path\n        or current_instance[\"task\"] != task_name\n    ):\n        # Update instance context if is not the same\n        product_name = self.get_product_name(\n            project_name=project_name,\n            project_entity=folder_entity,\n            folder_entity=folder_entity,\n            task_entity=task_entity,\n            variant=variant,\n            host_name=host_name,\n        )\n        current_instance[\"folderPath\"] = folder_path\n        current_instance[\"task\"] = task_name\n        current_instance[\"productName\"] = product_name\n    set_instance(\n        instance_id=current_instance.get(\"instance_id\"),\n        instance_data=current_instance.data_to_store()\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_workfile.html#client.ayon_marvelousdesigner.plugins.create.create_workfile.CreateWorkfile.create_instance_in_context","title":"<code>create_instance_in_context(product_name, data)</code>","text":"<p>Create a new instance in the current context.</p> <p>Parameters:</p> Name Type Description Default <code>product_name</code> <code>str</code> <p>The name of the product to create.</p> required <code>data</code> <code>dict</code> <p>The data for the instance.</p> required <p>Returns:</p> Name Type Description <code>CreatedInstance</code> <code>CreatedInstance</code> <p>The newly created instance.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/create/create_workfile.py</code> <pre><code>def create_instance_in_context(\n        self, product_name: str, data: dict) -&gt; CreatedInstance:\n    \"\"\"Create a new instance in the current context.\n\n    Args:\n        product_name (str): The name of the product to create.\n        data (dict): The data for the instance.\n\n    Returns:\n        CreatedInstance: The newly created instance.\n    \"\"\"\n    instance = CreatedInstance(\n        product_type=self.product_type,\n        product_name=product_name,\n        data=data,\n        creator=self\n    )\n    self.create_context.creator_adds_instance(instance)\n    return instance\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_workfile.html#client.ayon_marvelousdesigner.plugins.create.create_workfile.CreateWorkfile.create_instance_in_context_from_existing","title":"<code>create_instance_in_context_from_existing(data)</code>","text":"<p>Create an instance in the current context from existing data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>The existing instance data.</p> required <p>Returns:</p> Name Type Description <code>CreatedInstance</code> <code>CreatedInstance</code> <p>The created instance from existing data.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/create/create_workfile.py</code> <pre><code>def create_instance_in_context_from_existing(\n        self, data: dict) -&gt; CreatedInstance:\n    \"\"\"Create an instance in the current context from existing data.\n\n    Args:\n        data (dict): The existing instance data.\n\n    Returns:\n        CreatedInstance: The created instance from existing data.\n    \"\"\"\n    instance = CreatedInstance.from_existing(data, self)\n    self.create_context.creator_adds_instance(instance)\n    return instance\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_workfile.html#client.ayon_marvelousdesigner.plugins.create.create_workfile.CreateWorkfile.update_instances","title":"<code>update_instances(update_list)</code>","text":"<p>Update existing instances with new data.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/create/create_workfile.py</code> <pre><code>def update_instances(self, update_list: list) -&gt; None:  # noqa: PLR6301\n    \"\"\"Update existing instances with new data.\"\"\"\n    instance_data_by_id = {}\n    for instance, _changes in update_list:\n        # Persist the data\n        instance_id = instance.get(\"instance_id\")\n        instance_data = instance.data_to_store()\n        instance_data_by_id[instance_id] = instance_data\n    set_instances(instance_data_by_id, update=True)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_zfab.html","title":"create_zfab","text":"<p>Creator plugin for Marvelous Designer Zfab products.</p> <p>This module provides the CreateZFab creator class for generating zfab product instances in Marvelous Designer.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_zfab.html#client.ayon_marvelousdesigner.plugins.create.create_zfab.CreateZFab","title":"<code>CreateZFab</code>","text":"<p>               Bases: <code>MDCreator</code></p> <p>Model creator for Marvelous Designer.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/create/create_zfab.py</code> <pre><code>class CreateZFab(plugin.MDCreator):\n    \"\"\"Model creator for Marvelous Designer.\"\"\"\n    identifier = \"io.ayon.creators.marvelousdesigner.zfab\"\n    label = \"Zfab\"\n    product_type = \"zfab\"\n    product_base_type = \"zfab\"\n    icon = \"picture-o\"\n\n    def create(self, product_name: str,\n               instance_data: dict, pre_create_data: dict) -&gt; None:\n        \"\"\"Create a new zfab product instance in Marvelous Designer.\n\n        Args:\n        product_name(str): The name of the product to create.\n        instance_data(dict): Data associated with the instance.\n        pre_create_data(dict): Pre-creation configuration data.\n        \"\"\"\n        if pre_create_data.get(\"use_selection\"):\n            fabric_index = fabric_api.GetCurrentFabricIndex()\n            instance_data[\"fabricIndex\"] = fabric_index\n        else:\n            fabric_index = 1\n            instance_data[\"fabricIndex\"] = fabric_index\n\n        instance_data[\"fabricName\"] = fabric_api.GetFabricName(fabric_index)\n        instance = self.create_instance_in_context(product_name,\n                                                   instance_data)\n\n        set_instance(\n            instance_id=instance[\"instance_id\"],\n            instance_data=instance.data_to_store()\n        )\n\n    @staticmethod\n    def get_pre_create_attr_defs() -&gt; list:\n        \"\"\"Get pre-creation attribute definitions.\n\n        Returns:\n            list: List of attribute definitions for pre-creation configuration.\n        \"\"\"\n        return [\n            BoolDef(\"use_selection\", label=\"Use selection\")\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_zfab.html#client.ayon_marvelousdesigner.plugins.create.create_zfab.CreateZFab.create","title":"<code>create(product_name, instance_data, pre_create_data)</code>","text":"<p>Create a new zfab product instance in Marvelous Designer.</p> <p>Args: product_name(str): The name of the product to create. instance_data(dict): Data associated with the instance. pre_create_data(dict): Pre-creation configuration data.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/create/create_zfab.py</code> <pre><code>def create(self, product_name: str,\n           instance_data: dict, pre_create_data: dict) -&gt; None:\n    \"\"\"Create a new zfab product instance in Marvelous Designer.\n\n    Args:\n    product_name(str): The name of the product to create.\n    instance_data(dict): Data associated with the instance.\n    pre_create_data(dict): Pre-creation configuration data.\n    \"\"\"\n    if pre_create_data.get(\"use_selection\"):\n        fabric_index = fabric_api.GetCurrentFabricIndex()\n        instance_data[\"fabricIndex\"] = fabric_index\n    else:\n        fabric_index = 1\n        instance_data[\"fabricIndex\"] = fabric_index\n\n    instance_data[\"fabricName\"] = fabric_api.GetFabricName(fabric_index)\n    instance = self.create_instance_in_context(product_name,\n                                               instance_data)\n\n    set_instance(\n        instance_id=instance[\"instance_id\"],\n        instance_data=instance.data_to_store()\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/create/create_zfab.html#client.ayon_marvelousdesigner.plugins.create.create_zfab.CreateZFab.get_pre_create_attr_defs","title":"<code>get_pre_create_attr_defs()</code>  <code>staticmethod</code>","text":"<p>Get pre-creation attribute definitions.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>List of attribute definitions for pre-creation configuration.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/create/create_zfab.py</code> <pre><code>@staticmethod\ndef get_pre_create_attr_defs() -&gt; list:\n    \"\"\"Get pre-creation attribute definitions.\n\n    Returns:\n        list: List of attribute definitions for pre-creation configuration.\n    \"\"\"\n    return [\n        BoolDef(\"use_selection\", label=\"Use selection\")\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/load/index.html","title":"load","text":""},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/load/load_pointcache.html","title":"load_pointcache","text":"<p>Point cache loader plugin for Marvelous Designer integration.</p> <p>This module provides LoadPointCache class for loading various point cache formats (ABC, FBX, OBJ) into Marvelous Designer through the AYON pipeline.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/load/load_pointcache.html#client.ayon_marvelousdesigner.plugins.load.load_pointcache.LoadPointCache","title":"<code>LoadPointCache</code>","text":"<p>               Bases: <code>LoaderPlugin</code></p> <p>Load Pointcache for project.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/load/load_pointcache.py</code> <pre><code>class LoadPointCache(load.LoaderPlugin):\n    \"\"\"Load Pointcache for project.\"\"\"\n    product_types: ClassVar[set[str]] = {\"*\"}\n    representations: ClassVar[set[str]] = {\"abc\", \"fbx\", \"obj\"}\n\n    label = \"Load Pointcache\"\n    order = -10\n    icon = \"code-fork\"\n    color = \"orange\"\n\n    def load(self,\n             context: dict,\n             name: Optional[str] = None,\n             namespace: Optional[str] = None,\n             options: Optional[dict] = None) -&gt; None:\n        \"\"\"Load pointcache into the scene.\"\"\"\n        file_path = self._get_filepath(context)\n        extension = os.path.splitext(file_path)[-1].lower()\n        loaded_options = self.load_options(extension)\n        self.load_pointcache(file_path, extension, loaded_options)\n        containerise(\n            name=name,\n            namespace=namespace,\n            context=context,\n            loader=self\n        )\n\n    @staticmethod\n    def load_pointcache(\n        file_path: Path,\n        extension: str,\n        options: Union[ApiTypes.ImportAlembicOption,\n                       ApiTypes.ImportExportOption]) -&gt; None:\n        \"\"\"Actual loading logic for pointcache.\n\n        Args:\n            file_path (Path): Path to pointcache file.\n            extension (str): Extension of pointcache file.\n            options (ApiTypes.ImportExportOption): Options for loading.\n\n        Raises:\n            LoadError: If the pointcache format is unsupported.\n\n        \"\"\"\n        if extension == \".abc\":\n            import_api.ImportAlembic(file_path, options)\n        elif extension == \".fbx\":\n            import_api.ImportFBX(file_path, options)\n        elif extension == \".obj\":\n            import_api.ImportOBJ(file_path, options)\n        else:\n            msg = f\"Unsupported pointcache format: {extension}\"\n            raise LoadError(msg)\n\n    @staticmethod\n    def load_options(extension: str) -&gt; Union[\n            ApiTypes.ImportAlembicOption, ApiTypes.ImportExportOption]:\n        \"\"\"Return options for loading pointcache.\n\n        Args:\n            extension (str): Extension of pointcache file.\n\n        Returns:\n            Union[\n                ApiTypes.ImportAlembicOption,\n                ApiTypes.ImportExportOption]: Options for loading.\n\n        Raises:\n            LoadError: If the pointcache format is unsupported.\n\n        \"\"\"\n        if extension == \".abc\":\n            return ApiTypes.ImportAlembicOption()\n\n        if extension in {\".fbx\", \".obj\"}:\n            return ApiTypes.ImportExportOption()\n\n        msg = f\"Unsupported pointcache format: {extension}\"\n        raise LoadError(msg)\n\n    def _get_filepath(self, context: dict) -&gt; Path:\n        \"\"\"Gets filepath with either representation trait or context data.\n\n        For backward compatibility only.\n\n        Args:\n            context (dict): Context dictionary.\n\n        Returns:\n            Path: File path to load.\n\n        \"\"\"\n        traits_raw = context[\"representation\"].get(\"traits\")\n        if traits_raw is not None:\n            # construct Representation object from the context\n            representation = Representation.from_dict(\n                name=context[\"representation\"][\"name\"],\n                representation_id=context[\"representation\"][\"id\"],\n                trait_data=json.loads(traits_raw),\n            )\n\n            file_path: Path = representation.get_trait(FileLocation).file_path\n        else:\n            filepath = self.filepath_from_context(context)\n            file_path = Path(filepath).as_posix()\n\n        return file_path\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/load/load_pointcache.html#client.ayon_marvelousdesigner.plugins.load.load_pointcache.LoadPointCache.load","title":"<code>load(context, name=None, namespace=None, options=None)</code>","text":"<p>Load pointcache into the scene.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/load/load_pointcache.py</code> <pre><code>def load(self,\n         context: dict,\n         name: Optional[str] = None,\n         namespace: Optional[str] = None,\n         options: Optional[dict] = None) -&gt; None:\n    \"\"\"Load pointcache into the scene.\"\"\"\n    file_path = self._get_filepath(context)\n    extension = os.path.splitext(file_path)[-1].lower()\n    loaded_options = self.load_options(extension)\n    self.load_pointcache(file_path, extension, loaded_options)\n    containerise(\n        name=name,\n        namespace=namespace,\n        context=context,\n        loader=self\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/load/load_pointcache.html#client.ayon_marvelousdesigner.plugins.load.load_pointcache.LoadPointCache.load_options","title":"<code>load_options(extension)</code>  <code>staticmethod</code>","text":"<p>Return options for loading pointcache.</p> <p>Parameters:</p> Name Type Description Default <code>extension</code> <code>str</code> <p>Extension of pointcache file.</p> required <p>Returns:</p> Type Description <code>Union[ImportAlembicOption, ImportExportOption]</code> <p>Union[ ApiTypes.ImportAlembicOption, ApiTypes.ImportExportOption]: Options for loading.</p> <p>Raises:</p> Type Description <code>LoadError</code> <p>If the pointcache format is unsupported.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/load/load_pointcache.py</code> <pre><code>@staticmethod\ndef load_options(extension: str) -&gt; Union[\n        ApiTypes.ImportAlembicOption, ApiTypes.ImportExportOption]:\n    \"\"\"Return options for loading pointcache.\n\n    Args:\n        extension (str): Extension of pointcache file.\n\n    Returns:\n        Union[\n            ApiTypes.ImportAlembicOption,\n            ApiTypes.ImportExportOption]: Options for loading.\n\n    Raises:\n        LoadError: If the pointcache format is unsupported.\n\n    \"\"\"\n    if extension == \".abc\":\n        return ApiTypes.ImportAlembicOption()\n\n    if extension in {\".fbx\", \".obj\"}:\n        return ApiTypes.ImportExportOption()\n\n    msg = f\"Unsupported pointcache format: {extension}\"\n    raise LoadError(msg)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/load/load_pointcache.html#client.ayon_marvelousdesigner.plugins.load.load_pointcache.LoadPointCache.load_pointcache","title":"<code>load_pointcache(file_path, extension, options)</code>  <code>staticmethod</code>","text":"<p>Actual loading logic for pointcache.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>Path</code> <p>Path to pointcache file.</p> required <code>extension</code> <code>str</code> <p>Extension of pointcache file.</p> required <code>options</code> <code>ImportExportOption</code> <p>Options for loading.</p> required <p>Raises:</p> Type Description <code>LoadError</code> <p>If the pointcache format is unsupported.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/load/load_pointcache.py</code> <pre><code>@staticmethod\ndef load_pointcache(\n    file_path: Path,\n    extension: str,\n    options: Union[ApiTypes.ImportAlembicOption,\n                   ApiTypes.ImportExportOption]) -&gt; None:\n    \"\"\"Actual loading logic for pointcache.\n\n    Args:\n        file_path (Path): Path to pointcache file.\n        extension (str): Extension of pointcache file.\n        options (ApiTypes.ImportExportOption): Options for loading.\n\n    Raises:\n        LoadError: If the pointcache format is unsupported.\n\n    \"\"\"\n    if extension == \".abc\":\n        import_api.ImportAlembic(file_path, options)\n    elif extension == \".fbx\":\n        import_api.ImportFBX(file_path, options)\n    elif extension == \".obj\":\n        import_api.ImportOBJ(file_path, options)\n    else:\n        msg = f\"Unsupported pointcache format: {extension}\"\n        raise LoadError(msg)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/load/load_zfab.html","title":"load_zfab","text":"<p>Plugin to load ZFab files into Marvelous Designer.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/load/load_zfab.html#client.ayon_marvelousdesigner.plugins.load.load_zfab.LoadZfab","title":"<code>LoadZfab</code>","text":"<p>               Bases: <code>LoaderPlugin</code></p> <p>Load ZFab for project.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/load/load_zfab.py</code> <pre><code>class LoadZfab(load.LoaderPlugin):\n    \"\"\"Load ZFab for project.\"\"\"\n    product_types: ClassVar[set[str]] = {\"zfab\"}\n    representations: ClassVar[set[str]] = {\"zfab\"}\n\n    label = \"Load ZFab\"\n    order = -10\n    icon = \"code-fork\"\n    color = \"orange\"\n\n    def load(\n            self,\n            context: dict,\n            name: Optional[str] = None,\n            namespace: Optional[str] = None,\n            options: Optional[dict] = None) -&gt; None:\n        \"\"\"Load pointcache into the scene.\n\n        Args:\n            context (dict): Context dictionary with representation info.\n            name (str): Name of the container.\n            namespace (str): Namespace for the loaded data.\n            options (dict): Additional options for loading.\n\n        \"\"\"\n        file_path = self._get_filepath(context)\n\n        fabric_index = fabric_api.AddFabric(file_path)\n        containerise(\n            name=name,\n            namespace=namespace,\n            context=context,\n            loader=self,\n            options={\"fabricIndex\": fabric_index}\n        )\n\n    def update(self, container: dict, context: dict) -&gt; None:\n        \"\"\"Update loaded zfab in the scene.\n\n        Args:\n            container (dict): Container data.\n            context (dict): Context dictionary with representation info.\n\n        \"\"\"\n        file_path = self._get_filepath(context)\n\n        fabric_index = container.get(\"fabricIndex\")\n        if fabric_index is not None:\n            fabric_api.ReplaceFabric(fabric_index, file_path.as_posix())\n        imprint(container[\"objectName\"], {\n            \"representation\": context[\"representation\"][\"id\"],\n        })\n\n    def remove(self, container: dict) -&gt; None:  # noqa: PLR6301\n        \"\"\"Remove loaded zfab from the scene.\"\"\"\n        fabric_index = container.get(\"fabricIndex\")\n        if fabric_index is not None:\n            fabric_api.DeleteFabric(fabric_index)\n\n        remove_container_data(container[\"objectName\"])\n\n    def _get_filepath(self, context: dict) -&gt; Path:\n        \"\"\"Gets filepath with either representation trait or context data.\n\n        For backward compatibility only.\n\n        Args:\n            context (dict): Context dictionary.\n\n        Returns:\n            Path: File path to load.\n\n        \"\"\"\n        traits_raw = context[\"representation\"].get(\"traits\")\n        if traits_raw is not None:\n            # construct Representation object from the context\n            representation = Representation.from_dict(\n                name=context[\"representation\"][\"name\"],\n                representation_id=context[\"representation\"][\"id\"],\n                trait_data=json.loads(traits_raw),\n            )\n\n            file_path: Path = representation.get_trait(FileLocation).file_path\n        else:\n            filepath = self.filepath_from_context(context)\n            file_path = Path(filepath).as_posix()\n\n        return file_path\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/load/load_zfab.html#client.ayon_marvelousdesigner.plugins.load.load_zfab.LoadZfab.load","title":"<code>load(context, name=None, namespace=None, options=None)</code>","text":"<p>Load pointcache into the scene.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>dict</code> <p>Context dictionary with representation info.</p> required <code>name</code> <code>str</code> <p>Name of the container.</p> <code>None</code> <code>namespace</code> <code>str</code> <p>Namespace for the loaded data.</p> <code>None</code> <code>options</code> <code>dict</code> <p>Additional options for loading.</p> <code>None</code> Source code in <code>client/ayon_marvelousdesigner/plugins/load/load_zfab.py</code> <pre><code>def load(\n        self,\n        context: dict,\n        name: Optional[str] = None,\n        namespace: Optional[str] = None,\n        options: Optional[dict] = None) -&gt; None:\n    \"\"\"Load pointcache into the scene.\n\n    Args:\n        context (dict): Context dictionary with representation info.\n        name (str): Name of the container.\n        namespace (str): Namespace for the loaded data.\n        options (dict): Additional options for loading.\n\n    \"\"\"\n    file_path = self._get_filepath(context)\n\n    fabric_index = fabric_api.AddFabric(file_path)\n    containerise(\n        name=name,\n        namespace=namespace,\n        context=context,\n        loader=self,\n        options={\"fabricIndex\": fabric_index}\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/load/load_zfab.html#client.ayon_marvelousdesigner.plugins.load.load_zfab.LoadZfab.remove","title":"<code>remove(container)</code>","text":"<p>Remove loaded zfab from the scene.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/load/load_zfab.py</code> <pre><code>def remove(self, container: dict) -&gt; None:  # noqa: PLR6301\n    \"\"\"Remove loaded zfab from the scene.\"\"\"\n    fabric_index = container.get(\"fabricIndex\")\n    if fabric_index is not None:\n        fabric_api.DeleteFabric(fabric_index)\n\n    remove_container_data(container[\"objectName\"])\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/load/load_zfab.html#client.ayon_marvelousdesigner.plugins.load.load_zfab.LoadZfab.update","title":"<code>update(container, context)</code>","text":"<p>Update loaded zfab in the scene.</p> <p>Parameters:</p> Name Type Description Default <code>container</code> <code>dict</code> <p>Container data.</p> required <code>context</code> <code>dict</code> <p>Context dictionary with representation info.</p> required Source code in <code>client/ayon_marvelousdesigner/plugins/load/load_zfab.py</code> <pre><code>def update(self, container: dict, context: dict) -&gt; None:\n    \"\"\"Update loaded zfab in the scene.\n\n    Args:\n        container (dict): Container data.\n        context (dict): Context dictionary with representation info.\n\n    \"\"\"\n    file_path = self._get_filepath(context)\n\n    fabric_index = container.get(\"fabricIndex\")\n    if fabric_index is not None:\n        fabric_api.ReplaceFabric(fabric_index, file_path.as_posix())\n    imprint(container[\"objectName\"], {\n        \"representation\": context[\"representation\"][\"id\"],\n    })\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/index.html","title":"publish","text":""},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/collect_current_file.html","title":"collect_current_file","text":"<p>Plugin for collecting the current working file in Marvelous Designer.</p> <p>This module provides a Pyblish plugin that injects the current working file path into the publish context for Marvelous Designer projects.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/collect_current_file.html#client.ayon_marvelousdesigner.plugins.publish.collect_current_file.CollectCurrentFile","title":"<code>CollectCurrentFile</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Inject the current working file into context.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/collect_current_file.py</code> <pre><code>class CollectCurrentFile(pyblish.api.ContextPlugin):\n    \"\"\"Inject the current working file into context.\"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.49\n    label = \"Current Workfile\"\n    hosts: ClassVar[list[str]] = [\"marvelousdesigner\"]\n\n    def process(self, context: pyblish.api.Context) -&gt; None:\n        \"\"\"Process the context to inject current workfile path.\n\n        Args:\n            context (pyblish.api.Context): The publish context to modify.\n        \"\"\"\n        host = registered_host()\n        path = host.get_current_workfile()\n        if not path:\n            self.log.error(\"Scene is not saved.\")\n\n        context.data[\"currentFile\"] = path\n        self.log.debug(\"Current workfile: %s\", path)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/collect_current_file.html#client.ayon_marvelousdesigner.plugins.publish.collect_current_file.CollectCurrentFile.process","title":"<code>process(context)</code>","text":"<p>Process the context to inject current workfile path.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>The publish context to modify.</p> required Source code in <code>client/ayon_marvelousdesigner/plugins/publish/collect_current_file.py</code> <pre><code>def process(self, context: pyblish.api.Context) -&gt; None:\n    \"\"\"Process the context to inject current workfile path.\n\n    Args:\n        context (pyblish.api.Context): The publish context to modify.\n    \"\"\"\n    host = registered_host()\n    path = host.get_current_workfile()\n    if not path:\n        self.log.error(\"Scene is not saved.\")\n\n    context.data[\"currentFile\"] = path\n    self.log.debug(\"Current workfile: %s\", path)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/collect_export_option.html","title":"collect_export_option","text":"<p>Collect current work file.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/collect_export_option.html#client.ayon_marvelousdesigner.plugins.publish.collect_export_option.CollectExportOption","title":"<code>CollectExportOption</code>","text":"<p>               Bases: <code>InstancePlugin</code>, <code>AYONPyblishPluginMixin</code></p> <p>Inject the current export option into context.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/collect_export_option.py</code> <pre><code>class CollectExportOption(pyblish.api.InstancePlugin,\n                          AYONPyblishPluginMixin):\n    \"\"\"Inject the current export option into context.\"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.01\n    label = \"Collect Export Option\"\n    hosts: ClassVar[list[str]] = [\"marvelousdesigner\"]\n    families: ClassVar[list[str]] = [\"model\", \"pointcache\"]\n\n    def process(self, instance: pyblish.api.Instance) -&gt; None:\n        \"\"\"Inject the current export option.\n\n        Args:\n            instance (pyblish.api.Instance): The instance to process.\n        \"\"\"\n        attr_values = self.get_attr_values_from_data(instance.data)\n        export_option_data = {\n            \"bExportGarment\": attr_values.get(\"bExportGarment\", True),\n            \"bExportAvatar\": attr_values.get(\"bExportAvatar\", False),\n            \"bSingleObject\": attr_values.get(\"bSingleObject\", True),\n            \"bThin\": attr_values.get(\"bThin\", False),\n            \"bMetaData\": attr_values.get(\"bMetaData\", True),\n        }\n        instance.data[\"exportOptions\"] = export_option_data\n\n    @classmethod\n    def get_attribute_defs(cls) -&gt; list[BoolDef]:\n        \"\"\"Get attribute definitions for export options.\n\n        Returns:\n        list: List of boolean attribute definitions for export options.\n\n        \"\"\"\n        return [\n            UISeparatorDef(\"sep_export_options\"),\n            UILabelDef(\"Export Options\"),\n            BoolDef(\"bExportGarment\",\n                    label=\"Export Garment\",\n                    default=True),\n            BoolDef(\"bExportAvatar\",\n                    label=\"Export Avatar\",\n                    default=False),\n            BoolDef(\"bSingleObject\",\n                    label=\"Single Object\",\n                    default=True),\n            BoolDef(\"bThin\",\n                    label=\"Thin\",\n                    default=False),\n            BoolDef(\"bMetaData\",\n                    label=\"XML MetaData\",\n                    default=True),\n            UISeparatorDef(\"sep_export_options\"),\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/collect_export_option.html#client.ayon_marvelousdesigner.plugins.publish.collect_export_option.CollectExportOption.get_attribute_defs","title":"<code>get_attribute_defs()</code>  <code>classmethod</code>","text":"<p>Get attribute definitions for export options.</p> <p>Returns: list: List of boolean attribute definitions for export options.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/collect_export_option.py</code> <pre><code>@classmethod\ndef get_attribute_defs(cls) -&gt; list[BoolDef]:\n    \"\"\"Get attribute definitions for export options.\n\n    Returns:\n    list: List of boolean attribute definitions for export options.\n\n    \"\"\"\n    return [\n        UISeparatorDef(\"sep_export_options\"),\n        UILabelDef(\"Export Options\"),\n        BoolDef(\"bExportGarment\",\n                label=\"Export Garment\",\n                default=True),\n        BoolDef(\"bExportAvatar\",\n                label=\"Export Avatar\",\n                default=False),\n        BoolDef(\"bSingleObject\",\n                label=\"Single Object\",\n                default=True),\n        BoolDef(\"bThin\",\n                label=\"Thin\",\n                default=False),\n        BoolDef(\"bMetaData\",\n                label=\"XML MetaData\",\n                default=True),\n        UISeparatorDef(\"sep_export_options\"),\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/collect_export_option.html#client.ayon_marvelousdesigner.plugins.publish.collect_export_option.CollectExportOption.process","title":"<code>process(instance)</code>","text":"<p>Inject the current export option.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>The instance to process.</p> required Source code in <code>client/ayon_marvelousdesigner/plugins/publish/collect_export_option.py</code> <pre><code>def process(self, instance: pyblish.api.Instance) -&gt; None:\n    \"\"\"Inject the current export option.\n\n    Args:\n        instance (pyblish.api.Instance): The instance to process.\n    \"\"\"\n    attr_values = self.get_attr_values_from_data(instance.data)\n    export_option_data = {\n        \"bExportGarment\": attr_values.get(\"bExportGarment\", True),\n        \"bExportAvatar\": attr_values.get(\"bExportAvatar\", False),\n        \"bSingleObject\": attr_values.get(\"bSingleObject\", True),\n        \"bThin\": attr_values.get(\"bThin\", False),\n        \"bMetaData\": attr_values.get(\"bMetaData\", True),\n    }\n    instance.data[\"exportOptions\"] = export_option_data\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/collect_workfile.html","title":"collect_workfile","text":"<p>Collect current work file.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/collect_workfile.html#client.ayon_marvelousdesigner.plugins.publish.collect_workfile.CollectWorkfile","title":"<code>CollectWorkfile</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Inject the current working file into context.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/collect_workfile.py</code> <pre><code>class CollectWorkfile(pyblish.api.InstancePlugin):\n    \"\"\"Inject the current working file into context.\"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.01\n    label = \"Collect Workfile\"\n    hosts: ClassVar[list[str]] = [\"marvelousdesigner\"]\n    families: ClassVar[list[str]] = [\"workfile\"]\n\n    def process(self, instance: pyblish.api.Instance) -&gt; None:\n        \"\"\"Inject the current working file.\n\n        Args:\n            instance (pyblish.api.Instance): The instance to process.\n        \"\"\"\n        context = instance.context\n        current_file = context.data.get(\"currentFile\")\n        filepath = Path(current_file)\n        ayon_temp_dir = os.getenv(\"AYON_TEMP_DIR\", \"\")\n        if ayon_temp_dir and filepath.parent == ayon_temp_dir:\n            self.log.warning(\"Deactivating workfile instance because no \"\n                             \"current filepath is found. Please save your \"\n                             \"workfile.\")\n            instance.data[\"publish\"] = False\n            return\n\n        ext = filepath.suffix\n        instance.data.update(\n            {\n                \"setMembers\": [filepath.as_posix()],\n                \"frameStart\": context.data.get(\"frameStart\", 1),\n                \"frameEnd\": context.data.get(\"frameEnd\", 1),\n                \"handleStart\": context.data.get(\"handleStart\", 1),\n                \"handledEnd\": context.data.get(\"handleEnd\", 1),\n                \"representations\": [\n                    {\n                        \"name\": ext.lstrip(\".\"),\n                        \"ext\": ext.lstrip(\".\"),\n                        \"files\": filepath.name,\n                        \"stagingDir\": filepath.parent,\n                    }\n                ],\n            }\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/collect_workfile.html#client.ayon_marvelousdesigner.plugins.publish.collect_workfile.CollectWorkfile.process","title":"<code>process(instance)</code>","text":"<p>Inject the current working file.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>The instance to process.</p> required Source code in <code>client/ayon_marvelousdesigner/plugins/publish/collect_workfile.py</code> <pre><code>def process(self, instance: pyblish.api.Instance) -&gt; None:\n    \"\"\"Inject the current working file.\n\n    Args:\n        instance (pyblish.api.Instance): The instance to process.\n    \"\"\"\n    context = instance.context\n    current_file = context.data.get(\"currentFile\")\n    filepath = Path(current_file)\n    ayon_temp_dir = os.getenv(\"AYON_TEMP_DIR\", \"\")\n    if ayon_temp_dir and filepath.parent == ayon_temp_dir:\n        self.log.warning(\"Deactivating workfile instance because no \"\n                         \"current filepath is found. Please save your \"\n                         \"workfile.\")\n        instance.data[\"publish\"] = False\n        return\n\n    ext = filepath.suffix\n    instance.data.update(\n        {\n            \"setMembers\": [filepath.as_posix()],\n            \"frameStart\": context.data.get(\"frameStart\", 1),\n            \"frameEnd\": context.data.get(\"frameEnd\", 1),\n            \"handleStart\": context.data.get(\"handleStart\", 1),\n            \"handledEnd\": context.data.get(\"handleEnd\", 1),\n            \"representations\": [\n                {\n                    \"name\": ext.lstrip(\".\"),\n                    \"ext\": ext.lstrip(\".\"),\n                    \"files\": filepath.name,\n                    \"stagingDir\": filepath.parent,\n                }\n            ],\n        }\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.html","title":"extract_pointcache","text":"<p>Plugin for extracting point cache data from Marvelous Designer.</p> <p>This module provides extractors for exporting geometry and animation data in various formats (Alembic, OBJ, FBX) from Marvelous Designer.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.html#client.ayon_marvelousdesigner.plugins.publish.extract_pointcache.ExtractFbx","title":"<code>ExtractFbx</code>","text":"<p>               Bases: <code>ExtractPointCache</code></p> <p>Extract Geometry in FBX Format.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.py</code> <pre><code>class ExtractFbx(ExtractPointCache):\n    \"\"\"Extract Geometry in FBX Format.\"\"\"\n\n    label = \"Extract FBX\"\n    extension = \"fbx\"\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.html#client.ayon_marvelousdesigner.plugins.publish.extract_pointcache.ExtractObj","title":"<code>ExtractObj</code>","text":"<p>               Bases: <code>ExtractPointCache</code></p> <p>Extract PointCache in OBJ Format.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.py</code> <pre><code>class ExtractObj(ExtractPointCache):\n    \"\"\"Extract PointCache in OBJ Format.\"\"\"\n\n    label = \"Extract OBJ\"\n    extension = \"obj\"\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.html#client.ayon_marvelousdesigner.plugins.publish.extract_pointcache.ExtractPointCache","title":"<code>ExtractPointCache</code>","text":"<p>               Bases: <code>Extractor</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Extract Geometry in Alembic Format.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.py</code> <pre><code>class ExtractPointCache(publish.Extractor, OptionalPyblishPluginMixin):\n    \"\"\"Extract Geometry in Alembic Format.\"\"\"\n\n    order = pyblish.api.ExtractorOrder - 0.05\n    label = \"Extract PointCache (Alembic)\"\n    hosts: ClassVar[list[str]] = [\"marvelousdesigner\"]\n    families: ClassVar[list[str]] = [\"model\", \"pointcache\"]\n    optional = True\n    extension = \"abc\"\n\n    def process(self, instance: pyblish.api.Instance) -&gt; None:\n        \"\"\"Process the instance to extract point cache data.\n\n        Args:\n            instance (pyblish.api.Instance): The instance to process\n            for extraction.\n\n        Raises:\n            KnownPublishError: If the output file wasn't produced by\n            Marvelous Designer.\n        \"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        stagingdir = self.staging_dir(instance)\n        filename = f\"{instance.name}.{self.extension}\"\n        xml_filename = f\"{instance.name}.xml\"\n        filepath = Path(stagingdir) / filename\n\n        xml_output = Path(stagingdir) / xml_filename\n        export_option = self.export_option(instance)\n\n        output_files = self._export_mesh(filepath.as_posix(), export_option)\n        if not output_files:\n            msg = (\n                f\"Files [{output_files}] wasn't produced by Marvelous \"\n                \"Designer, please check the logs.\"\n            )\n            raise KnownPublishError(\n                msg\n            )\n        up_axis = self.get_up_axis(export_option)\n        rep = Representation(\n            self.extension,\n            traits=[\n                Static(),\n                FileLocation(file_path=Path(stagingdir) / filename),\n                Persistent(),\n                Geometry(),\n                Spatial(\n                    up_axis=up_axis,\n                    handedness=\"right\",\n                    meters_per_unit=export_option.scale / 100.0,\n                ),\n            ],\n        )\n\n        try:\n            rep.validate()\n        except TraitValidationError as e:\n            msg = f\"Representation {rep.name} is invalid: {e}\"\n            self.log.exception(msg)\n        finally:\n            add_trait_representations(instance, [rep])\n\n        self.log.info(\n            \"Extracted instance '%: %s' to: %s\",\n            instance.name,\n            rep.name,\n            rep.get_trait(FileLocation).file_path)\n\n        if os.path.exists(xml_output):\n\n            xml_rep = Representation(\n                \"xml\",\n                traits=[\n                    Static(),\n                    FileLocation(file_path=Path(stagingdir) / xml_filename),\n                    Persistent(),\n                ],\n            )\n\n            try:\n                rep.validate()\n            except TraitValidationError as e:\n                msg = f\"Representation {xml_rep.name} is invalid: {e}\"\n                self.log.exception(msg)\n            finally:\n                add_trait_representations(instance, [xml_rep])\n\n            self.log.info(\n                \"Extracted instance '%: %s' to: %s\",\n                instance.name,\n                xml_rep.name,\n                xml_rep.get_trait(FileLocation).file_path,\n            )\n\n    def _export_mesh(\n            self,\n            filepath: str,\n            export_options: ApiTypes.ImportExportOption\n        ) -&gt; str:\n        \"\"\"Export mesh to filepath.\n\n        Args:\n            filepath (str): filepath\n            export_options (ApiTypes.ImportExportOption): export options\n\n        Returns:\n            str: Output file paths\n\n        Raises:\n            KnownPublishError: If the export format is not supported.\n        \"\"\"\n        if self.extension == \"abc\":\n            return export_api.ExportAlembicW(filepath, export_options)\n        if self.extension == \"fbx\":\n            return export_api.ExportFBXW(filepath, export_options)\n        if self.extension == \"obj\":\n            return export_api.ExportOBJW(filepath, export_options)\n\n        msg = f\"Unsupported export format: {self.extension}\"\n        raise KnownPublishError(msg)\n\n    @staticmethod\n    def export_option(\n            instance: pyblish.api.Instance\n        ) -&gt; ApiTypes.ImportExportOption:\n        \"\"\"Get export options for point cache export.\n\n        Returns:\n            ApiTypes.ImportExportOption: export options\n        \"\"\"\n        export_option = ApiTypes.ImportExportOption()\n        options = instance.data[\"exportOptions\"]\n        export_option.bExportGarment = options.get(\"bExportGarment\", True)\n        export_option.bExportAvatar = options.get(\"bExportAvatar\", False)\n        export_option.bSingleObject = options.get(\"bSingleObject\", True)\n        export_option.bThin = options.get(\"bThin\", False)\n        export_option.bMetaData = options.get(\"bMetaData\", True)\n        return export_option\n\n    @staticmethod\n    def get_up_axis(\n            export_option: ApiTypes.ImportExportOption\n        ) -&gt; str:\n        \"\"\"Determine the up axis based on export options.\n\n        Args:\n            export_option (ApiTypes.ImportExportOption): Export options.\n\n        Returns:\n            str: The up axis (\"Y\" or \"Z\").\n        \"\"\"\n        if export_option.axisY == 1:\n            return \"Y\"\n        if export_option.axisZ == 1:\n            return \"Z\"\n        if export_option.axisX == 1:\n            return \"X\"\n        return \"Y\"  # Default to Y if not specified\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.html#client.ayon_marvelousdesigner.plugins.publish.extract_pointcache.ExtractPointCache.export_option","title":"<code>export_option(instance)</code>  <code>staticmethod</code>","text":"<p>Get export options for point cache export.</p> <p>Returns:</p> Type Description <code>ImportExportOption</code> <p>ApiTypes.ImportExportOption: export options</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.py</code> <pre><code>@staticmethod\ndef export_option(\n        instance: pyblish.api.Instance\n    ) -&gt; ApiTypes.ImportExportOption:\n    \"\"\"Get export options for point cache export.\n\n    Returns:\n        ApiTypes.ImportExportOption: export options\n    \"\"\"\n    export_option = ApiTypes.ImportExportOption()\n    options = instance.data[\"exportOptions\"]\n    export_option.bExportGarment = options.get(\"bExportGarment\", True)\n    export_option.bExportAvatar = options.get(\"bExportAvatar\", False)\n    export_option.bSingleObject = options.get(\"bSingleObject\", True)\n    export_option.bThin = options.get(\"bThin\", False)\n    export_option.bMetaData = options.get(\"bMetaData\", True)\n    return export_option\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.html#client.ayon_marvelousdesigner.plugins.publish.extract_pointcache.ExtractPointCache.get_up_axis","title":"<code>get_up_axis(export_option)</code>  <code>staticmethod</code>","text":"<p>Determine the up axis based on export options.</p> <p>Parameters:</p> Name Type Description Default <code>export_option</code> <code>ImportExportOption</code> <p>Export options.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The up axis (\"Y\" or \"Z\").</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.py</code> <pre><code>@staticmethod\ndef get_up_axis(\n        export_option: ApiTypes.ImportExportOption\n    ) -&gt; str:\n    \"\"\"Determine the up axis based on export options.\n\n    Args:\n        export_option (ApiTypes.ImportExportOption): Export options.\n\n    Returns:\n        str: The up axis (\"Y\" or \"Z\").\n    \"\"\"\n    if export_option.axisY == 1:\n        return \"Y\"\n    if export_option.axisZ == 1:\n        return \"Z\"\n    if export_option.axisX == 1:\n        return \"X\"\n    return \"Y\"  # Default to Y if not specified\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.html#client.ayon_marvelousdesigner.plugins.publish.extract_pointcache.ExtractPointCache.process","title":"<code>process(instance)</code>","text":"<p>Process the instance to extract point cache data.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>The instance to process</p> required <p>Raises:</p> Type Description <code>KnownPublishError</code> <p>If the output file wasn't produced by</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/extract_pointcache.py</code> <pre><code>def process(self, instance: pyblish.api.Instance) -&gt; None:\n    \"\"\"Process the instance to extract point cache data.\n\n    Args:\n        instance (pyblish.api.Instance): The instance to process\n        for extraction.\n\n    Raises:\n        KnownPublishError: If the output file wasn't produced by\n        Marvelous Designer.\n    \"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    stagingdir = self.staging_dir(instance)\n    filename = f\"{instance.name}.{self.extension}\"\n    xml_filename = f\"{instance.name}.xml\"\n    filepath = Path(stagingdir) / filename\n\n    xml_output = Path(stagingdir) / xml_filename\n    export_option = self.export_option(instance)\n\n    output_files = self._export_mesh(filepath.as_posix(), export_option)\n    if not output_files:\n        msg = (\n            f\"Files [{output_files}] wasn't produced by Marvelous \"\n            \"Designer, please check the logs.\"\n        )\n        raise KnownPublishError(\n            msg\n        )\n    up_axis = self.get_up_axis(export_option)\n    rep = Representation(\n        self.extension,\n        traits=[\n            Static(),\n            FileLocation(file_path=Path(stagingdir) / filename),\n            Persistent(),\n            Geometry(),\n            Spatial(\n                up_axis=up_axis,\n                handedness=\"right\",\n                meters_per_unit=export_option.scale / 100.0,\n            ),\n        ],\n    )\n\n    try:\n        rep.validate()\n    except TraitValidationError as e:\n        msg = f\"Representation {rep.name} is invalid: {e}\"\n        self.log.exception(msg)\n    finally:\n        add_trait_representations(instance, [rep])\n\n    self.log.info(\n        \"Extracted instance '%: %s' to: %s\",\n        instance.name,\n        rep.name,\n        rep.get_trait(FileLocation).file_path)\n\n    if os.path.exists(xml_output):\n\n        xml_rep = Representation(\n            \"xml\",\n            traits=[\n                Static(),\n                FileLocation(file_path=Path(stagingdir) / xml_filename),\n                Persistent(),\n            ],\n        )\n\n        try:\n            rep.validate()\n        except TraitValidationError as e:\n            msg = f\"Representation {xml_rep.name} is invalid: {e}\"\n            self.log.exception(msg)\n        finally:\n            add_trait_representations(instance, [xml_rep])\n\n        self.log.info(\n            \"Extracted instance '%: %s' to: %s\",\n            instance.name,\n            xml_rep.name,\n            xml_rep.get_trait(FileLocation).file_path,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/extract_zfab.html","title":"extract_zfab","text":"<p>Extract zfab Format Plugin for Marvelous Designer in Ayon.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/extract_zfab.html#client.ayon_marvelousdesigner.plugins.publish.extract_zfab.ExtractZFab","title":"<code>ExtractZFab</code>","text":"<p>               Bases: <code>Extractor</code></p> <p>Extract zfab Format.</p> <p>Contains the property values (texture + physical properties) of the set Fabric.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/extract_zfab.py</code> <pre><code>class ExtractZFab(publish.Extractor):\n    \"\"\"Extract zfab Format.\n\n    Contains the property values (texture + physical properties)\n    of the set Fabric.\n    \"\"\"\n    order = pyblish.api.ExtractorOrder - 0.05\n    label = \"Extract Zfab\"\n    hosts: ClassVar[list[str]] = [\"marvelousdesigner\"]\n    families: ClassVar[list[str]] = [\"zfab\"]\n\n    def process(self, instance: pyblish.api.Instance) -&gt; None:\n        \"\"\"Process the instance to extract zfab data.\"\"\"\n        stagingdir = self.staging_dir(instance)\n        extension = \"zfab\"\n        filename = f\"{instance.name}.{extension}\"\n        filepath = os.path.join(stagingdir, filename)\n        target_fabric_index = instance.data[\"fabricIndex\"]\n\n        fabric_api.ExportZFab(filepath, target_fabric_index)\n\n        rep = Representation(extension, traits=[\n            FileLocation(\n                file_path=Path(stagingdir) / filename),\n            Static(),\n            Persistent(),\n        ])\n        add_trait_representations(\n            instance,\n            [rep],\n        )\n\n        self.log.info(\n            \"Extracted instance '%: %s' to: %s\",\n            instance.name,\n            rep.name,\n            rep.get_trait(FileLocation).file_path,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/extract_zfab.html#client.ayon_marvelousdesigner.plugins.publish.extract_zfab.ExtractZFab.process","title":"<code>process(instance)</code>","text":"<p>Process the instance to extract zfab data.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/extract_zfab.py</code> <pre><code>def process(self, instance: pyblish.api.Instance) -&gt; None:\n    \"\"\"Process the instance to extract zfab data.\"\"\"\n    stagingdir = self.staging_dir(instance)\n    extension = \"zfab\"\n    filename = f\"{instance.name}.{extension}\"\n    filepath = os.path.join(stagingdir, filename)\n    target_fabric_index = instance.data[\"fabricIndex\"]\n\n    fabric_api.ExportZFab(filepath, target_fabric_index)\n\n    rep = Representation(extension, traits=[\n        FileLocation(\n            file_path=Path(stagingdir) / filename),\n        Static(),\n        Persistent(),\n    ])\n    add_trait_representations(\n        instance,\n        [rep],\n    )\n\n    self.log.info(\n        \"Extracted instance '%: %s' to: %s\",\n        instance.name,\n        rep.name,\n        rep.get_trait(FileLocation).file_path,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/increment_workfile.html","title":"increment_workfile","text":"<p>Pyblish plugin for incrementing workfile version after successful publish.</p> <p>This module contains a plugin that automatically increments the current workfile version number after a successful publish operation in MD.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/increment_workfile.html#client.ayon_marvelousdesigner.plugins.publish.increment_workfile.IncrementWorkfileVersion","title":"<code>IncrementWorkfileVersion</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Increment current workfile version.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/increment_workfile.py</code> <pre><code>class IncrementWorkfileVersion(pyblish.api.ContextPlugin):\n    \"\"\"Increment current workfile version.\"\"\"\n\n    order = pyblish.api.IntegratorOrder + 1\n    label = \"Increment Workfile Version\"\n    optional = True\n    hosts: ClassVar[list[str]] = [\"marvelousdesigner\"]\n\n    def process(self, context: pyblish.api.Context) -&gt; None:\n        \"\"\"Process the context to increment the workfile version.\n\n        Args:\n            context (pyblish.api.Context): The publishing context.\n\n        Raises:\n            KnownPublishError: If the publishing was not successful.\n        \"\"\"\n        if not all(result[\"success\"] for result in context.data[\"results\"]):\n            msg = \"Publishing not successful so version is not increased.\"\n            raise KnownPublishError(msg)\n\n        host = registered_host()\n        current_filepath = context.data[\"currentFile\"]\n        try:\n            from ayon_core.host.interfaces import SaveWorkfileOptionalData\n            from ayon_core.pipeline.workfile import save_next_version\n\n            current_filename = os.path.basename(current_filepath)\n            save_next_version(\n                description=(\n                    f\"Incremented by publishing from {current_filename}\"\n                ),\n                # Optimize the save by reducing needed queries for context\n                prepared_data=SaveWorkfileOptionalData(\n                    project_entity=context.data[\"projectEntity\"],\n                    project_settings=context.data[\"project_settings\"],\n                    anatomy=context.data[\"anatomy\"],\n                )\n            )\n            new_filepath = host.get_current_workfile()\n\n        except ImportError:\n            # Backwards compatibility before ayon-core 1.5.0\n            self.log.debug(\n                \"Using legacy `version_up`. Update AYON core addon to \"\n                \"use newer `save_next_version` function.\"\n            )\n            new_filepath = version_up(current_filepath)\n            host.save_workfile(new_filepath)\n\n        self.log.info(\"Incrementing current workfile to: %s\", new_filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/increment_workfile.html#client.ayon_marvelousdesigner.plugins.publish.increment_workfile.IncrementWorkfileVersion.process","title":"<code>process(context)</code>","text":"<p>Process the context to increment the workfile version.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>The publishing context.</p> required <p>Raises:</p> Type Description <code>KnownPublishError</code> <p>If the publishing was not successful.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/increment_workfile.py</code> <pre><code>def process(self, context: pyblish.api.Context) -&gt; None:\n    \"\"\"Process the context to increment the workfile version.\n\n    Args:\n        context (pyblish.api.Context): The publishing context.\n\n    Raises:\n        KnownPublishError: If the publishing was not successful.\n    \"\"\"\n    if not all(result[\"success\"] for result in context.data[\"results\"]):\n        msg = \"Publishing not successful so version is not increased.\"\n        raise KnownPublishError(msg)\n\n    host = registered_host()\n    current_filepath = context.data[\"currentFile\"]\n    try:\n        from ayon_core.host.interfaces import SaveWorkfileOptionalData\n        from ayon_core.pipeline.workfile import save_next_version\n\n        current_filename = os.path.basename(current_filepath)\n        save_next_version(\n            description=(\n                f\"Incremented by publishing from {current_filename}\"\n            ),\n            # Optimize the save by reducing needed queries for context\n            prepared_data=SaveWorkfileOptionalData(\n                project_entity=context.data[\"projectEntity\"],\n                project_settings=context.data[\"project_settings\"],\n                anatomy=context.data[\"anatomy\"],\n            )\n        )\n        new_filepath = host.get_current_workfile()\n\n    except ImportError:\n        # Backwards compatibility before ayon-core 1.5.0\n        self.log.debug(\n            \"Using legacy `version_up`. Update AYON core addon to \"\n            \"use newer `save_next_version` function.\"\n        )\n        new_filepath = version_up(current_filepath)\n        host.save_workfile(new_filepath)\n\n    self.log.info(\"Incrementing current workfile to: %s\", new_filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/save_workfile.html","title":"save_workfile","text":"<p>Save current workfile plugin for Marvelous Designer.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/save_workfile.html#client.ayon_marvelousdesigner.plugins.publish.save_workfile.SaveCurrentWorkfile","title":"<code>SaveCurrentWorkfile</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Save current workfile.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/save_workfile.py</code> <pre><code>class SaveCurrentWorkfile(pyblish.api.ContextPlugin):\n    \"\"\"Save current workfile.\"\"\"\n\n    label = \"Save current workfile\"\n    order = pyblish.api.ExtractorOrder - 0.49\n    hosts: ClassVar[list[str]] = [\"marvelousdesigner\"]\n\n    def process(self, context: pyblish.api.Context) -&gt; None:\n        \"\"\"Process the context to save the current workfile.\n\n        Args:\n            context (pyblish.api.Context): The publishing context containing\n            current file information.\n\n        Raises:\n            KnownPublishError: If the workfile has changed during publishing.\n        \"\"\"\n        host = registered_host()\n        current = host.get_current_workfile()\n        if context.data[\"currentFile\"] != current:\n            msg = \"Workfile has changed during publishing!\"\n            raise KnownPublishError(msg)\n\n        if host.workfile_has_unsaved_changes():\n            self.log.info(\"Saving current file: %s\", current)\n            host.save_workfile(current)\n        else:\n            self.log.debug(\"No unsaved changes, skipping file save.\")\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/save_workfile.html#client.ayon_marvelousdesigner.plugins.publish.save_workfile.SaveCurrentWorkfile.process","title":"<code>process(context)</code>","text":"<p>Process the context to save the current workfile.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>The publishing context containing</p> required <p>Raises:</p> Type Description <code>KnownPublishError</code> <p>If the workfile has changed during publishing.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/save_workfile.py</code> <pre><code>def process(self, context: pyblish.api.Context) -&gt; None:\n    \"\"\"Process the context to save the current workfile.\n\n    Args:\n        context (pyblish.api.Context): The publishing context containing\n        current file information.\n\n    Raises:\n        KnownPublishError: If the workfile has changed during publishing.\n    \"\"\"\n    host = registered_host()\n    current = host.get_current_workfile()\n    if context.data[\"currentFile\"] != current:\n        msg = \"Workfile has changed during publishing!\"\n        raise KnownPublishError(msg)\n\n    if host.workfile_has_unsaved_changes():\n        self.log.info(\"Saving current file: %s\", current)\n        host.save_workfile(current)\n    else:\n        self.log.debug(\"No unsaved changes, skipping file save.\")\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/validate_no_fabric.html","title":"validate_no_fabric","text":"<p>Validate that workfile was saved before publishing.</p>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/validate_no_fabric.html#client.ayon_marvelousdesigner.plugins.publish.validate_no_fabric.ValidateNoFabric","title":"<code>ValidateNoFabric</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Validate that workfile was saved.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/validate_no_fabric.py</code> <pre><code>class ValidateNoFabric(pyblish.api.InstancePlugin):\n    \"\"\"Validate that workfile was saved.\"\"\"\n\n    order = pyblish.api.ValidatorOrder\n    hosts: ClassVar = [\"marvelousdesigner\"]\n    families: ClassVar = [\"zfab\"]\n    label = \"Validate No Fabric\"\n    actions: ClassVar = [RepairAction]\n\n    def process(self, instance: pyblish.api.Instance) -&gt; None:  # noqa: PLR6301\n        \"\"\"Process the instance to validate no fabric is selected.\n\n        Args:\n            instance (pyblish.api.Instance): The instance to validate.\n\n        Raises:\n            PublishValidationError: If a fabric is selected in the scene.\n        \"\"\"\n        fabric_index = instance.data[\"fabricIndex\"]\n        fabric_name = instance.data[\"fabricName\"]\n        if fabric_api.GetFabricName(fabric_index) != fabric_name:\n            msg = (\n                f\"Fabric '{fabric_name}' does not exist in the scene. \"\n                \"Please reselect any fabric you want to publish \"\n                \"and click 'Repair' action so that Ayon can reset for you.\"\n            )\n            raise PublishValidationError(\n                msg\n            )\n\n    @classmethod\n    def repair(cls, instance: pyblish.api.Instance) -&gt; None:\n        \"\"\"Repair the instance by resetting the fabric index.\"\"\"\n        fabric_index = fabric_api.GetCurrentFabricIndex()\n        instance.data[\"fabricIndex\"] = fabric_index\n        instance.data[\"fabricName\"] = fabric_api.GetFabricName(fabric_index)\n        cls.log.info(\n            f\"Reset fabric to '{instance.data['fabricName']}' \"  # noqa: G004\n            \"in the instance data.\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/validate_no_fabric.html#client.ayon_marvelousdesigner.plugins.publish.validate_no_fabric.ValidateNoFabric.process","title":"<code>process(instance)</code>","text":"<p>Process the instance to validate no fabric is selected.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>The instance to validate.</p> required <p>Raises:</p> Type Description <code>PublishValidationError</code> <p>If a fabric is selected in the scene.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/validate_no_fabric.py</code> <pre><code>def process(self, instance: pyblish.api.Instance) -&gt; None:  # noqa: PLR6301\n    \"\"\"Process the instance to validate no fabric is selected.\n\n    Args:\n        instance (pyblish.api.Instance): The instance to validate.\n\n    Raises:\n        PublishValidationError: If a fabric is selected in the scene.\n    \"\"\"\n    fabric_index = instance.data[\"fabricIndex\"]\n    fabric_name = instance.data[\"fabricName\"]\n    if fabric_api.GetFabricName(fabric_index) != fabric_name:\n        msg = (\n            f\"Fabric '{fabric_name}' does not exist in the scene. \"\n            \"Please reselect any fabric you want to publish \"\n            \"and click 'Repair' action so that Ayon can reset for you.\"\n        )\n        raise PublishValidationError(\n            msg\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_marvelousdesigner/plugins/publish/validate_no_fabric.html#client.ayon_marvelousdesigner.plugins.publish.validate_no_fabric.ValidateNoFabric.repair","title":"<code>repair(instance)</code>  <code>classmethod</code>","text":"<p>Repair the instance by resetting the fabric index.</p> Source code in <code>client/ayon_marvelousdesigner/plugins/publish/validate_no_fabric.py</code> <pre><code>@classmethod\ndef repair(cls, instance: pyblish.api.Instance) -&gt; None:\n    \"\"\"Repair the instance by resetting the fabric index.\"\"\"\n    fabric_index = fabric_api.GetCurrentFabricIndex()\n    instance.data[\"fabricIndex\"] = fabric_index\n    instance.data[\"fabricName\"] = fabric_api.GetFabricName(fabric_index)\n    cls.log.info(\n        f\"Reset fabric to '{instance.data['fabricName']}' \"  # noqa: G004\n        \"in the instance data.\"\n    )\n</code></pre>"},{"location":"autoapi/server/index.html","title":"server","text":"<p>Server package.</p>"},{"location":"autoapi/server/index.html#server.MarvelousDesignerAddon","title":"<code>MarvelousDesignerAddon</code>","text":"<p>               Bases: <code>BaseServerAddon</code></p> <p>Add-on class for the server.</p> Source code in <code>server/__init__.py</code> <pre><code>class MarvelousDesignerAddon(BaseServerAddon):  # noqa: RUF067\n    \"\"\"Add-on class for the server.\"\"\"\n    settings_model: Type[MarvelousDesignerSettings] = MarvelousDesignerSettings\n\n    async def get_default_settings(self) -&gt; MarvelousDesignerSettings:\n        \"\"\"Return default settings.\n\n        Returns:\n            MarvelousDesignerSettings: Default settings instance.\n        \"\"\"\n        settings_model_cls = self.get_settings_model()\n        return settings_model_cls(**DEFAULT_MD_VALUES)\n</code></pre>"},{"location":"autoapi/server/index.html#server.MarvelousDesignerAddon.get_default_settings","title":"<code>get_default_settings()</code>  <code>async</code>","text":"<p>Return default settings.</p> <p>Returns:</p> Name Type Description <code>MarvelousDesignerSettings</code> <code>MarvelousDesignerSettings</code> <p>Default settings instance.</p> Source code in <code>server/__init__.py</code> <pre><code>async def get_default_settings(self) -&gt; MarvelousDesignerSettings:\n    \"\"\"Return default settings.\n\n    Returns:\n        MarvelousDesignerSettings: Default settings instance.\n    \"\"\"\n    settings_model_cls = self.get_settings_model()\n    return settings_model_cls(**DEFAULT_MD_VALUES)\n</code></pre>"},{"location":"autoapi/server/settings.html","title":"settings","text":"<p>Settings for the addon.</p>"},{"location":"autoapi/server/settings.html#server.settings.BasicValidateModel","title":"<code>BasicValidateModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Basic model for publisher settings validation.</p> Source code in <code>server/settings.py</code> <pre><code>class BasicValidateModel(BaseSettingsModel):\n    \"\"\"Basic model for publisher settings validation.\"\"\"\n    enabled: bool = SettingsField(title=\"Enabled\")\n    optional: bool = SettingsField(title=\"Optional\")\n    active: bool = SettingsField(title=\"Active\")\n</code></pre>"},{"location":"autoapi/server/settings.html#server.settings.MarvelousDesignerSettings","title":"<code>MarvelousDesignerSettings</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Settings for the Marvelous Designer addon.</p> Source code in <code>server/settings.py</code> <pre><code>class MarvelousDesignerSettings(BaseSettingsModel):\n    \"\"\"Settings for the Marvelous Designer addon.\"\"\"\n    prelaunch_settings: PrelaunchModel = SettingsField(\n        default_factory=PrelaunchModel,\n        title=\"Prelaunch Settings\"\n    )\n    publish: PublishersModel = SettingsField(\n        default_factory=PublishersModel,\n        title=\"Publishers\"\n    )\n</code></pre>"},{"location":"autoapi/server/settings.html#server.settings.PrelaunchModel","title":"<code>PrelaunchModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Settings for prelaunch configuration.</p> Source code in <code>server/settings.py</code> <pre><code>class PrelaunchModel(BaseSettingsModel):\n    \"\"\"Settings for prelaunch configuration.\"\"\"\n    qt_binding_dir: str = SettingsField(\n        default=\"\",\n        title=\"Qt Binding Directory\"\n    )\n</code></pre>"},{"location":"autoapi/server/settings.html#server.settings.PublishersModel","title":"<code>PublishersModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Settings for publishers configuration.</p> Source code in <code>server/settings.py</code> <pre><code>class PublishersModel(BaseSettingsModel):\n    \"\"\"Settings for publishers configuration.\"\"\"\n    ExtractPointCache: BasicValidateModel = SettingsField(\n        default_factory=BasicValidateModel,\n        title=\"Extract Point Cache\"\n    )\n    ExtractObj: BasicValidateModel = SettingsField(\n        default_factory=BasicValidateModel,\n        title=\"Extract OBJ\"\n    )\n    ExtractFbx: BasicValidateModel = SettingsField(\n        default_factory=BasicValidateModel,\n        title=\"Extract FBX\"\n    )\n</code></pre>"}]}